Admin Feedback Phases 2 3 Validation
branch=admin-work
commit=590c42e31c76391deca36d3402cb6a09216af71e
targeted_status=1
lint_status=0
build_status=0
diff_status=0

=== TARGETED ===
bun test v1.3.14 (0d9b296a)

::group::tests/admin-incidents-multisite.test.ts:
(pass) Incidencias multisede > reutiliza las sedes cacheadas y carga las faltantes en orden canónico [0.62ms]
(pass) Incidencias multisede > si todas las sedes están cacheadas no realiza nuevas lecturas [0.14ms]
(pass) Incidencias multisede > el toggle activa, desactiva y se invalida al cambiar la sede del Shell [0.08ms]
(pass) Incidencias multisede > usa la expiración más temprana para refrescar el agregado multisede [0.10ms]
(pass) Incidencias multisede > un fallo conserva las sedes ya cargadas y el reintento pide solo la faltante [0.23ms]
(pass) Incidencias multisede > agrega por family_key, conserva fuentes y aplica la precedencia de estado [0.55ms]
(pass) Incidencias multisede > la mutación confirmada conserva el summary, actualiza revisión y evita relecturas [1.39ms]
(pass) Incidencias multisede > una mutación global invalida summaries de todas las sedes [0.32ms]
(pass) Incidencias multisede > un fallo de mutación no altera la familia cacheada [0.52ms]
(pass) Incidencias multisede > propose_delete actualiza deletion_proposed en todos los summaries cacheados [0.47ms]
200 |     expect(source).toContain('onConfirm={() => proposeDeleteSource(deleteProposal.source)}');
201 |     expect(source).toContain('family.family_state === "pendiente"');
202 |     expect(source).toContain('scope: "site"');
203 |     expect(source).toContain('summary.revisions.incidents');
204 |     expect(source).toContain('Detectado en: <strong>{proposal.source.siteName}</strong>');
205 |     expect(source).toMatch(/void onConfirm\(\)\s*\.then\(onClose\)\s*\.catch/);
                         ^
error: expect(received).toMatch(expected)

Expected substring or pattern: /void onConfirm\(\)\s*\.then\(onClose\)\s*\.catch/
Received: "import { useCallback, useEffect, useMemo, useRef, useState } from \"react\";\nimport {\n  AlarmClockOff,\n  ChevronLeft,\n  ChevronRight,\n  CircleOff,\n  Eye,\n  RotateCcw,\n} from \"lucide-react\";\nimport { useAdminStore } from \"../admin.v2.context\";\nimport { useManagement, useManagementQuery } from \"../admin.management.context\";\nimport { AdminDialog } from \"../admin.dialog\";\nimport { ReadNotice } from \"../admin.management.presentation\";\nimport { AdminNotice } from \"../admin.primitives\";\nimport {\n  incidentSiteAbbreviation,\n  incidentTimestamp,\n} from \"./admin.incidents.presentation\";\nimport { orderedAdminSites } from \"../admin.site-ui\";\nimport {\n  incidentAllScopeActive,\n  loadIncidentSummaries,\n  mergeIncidentSummaries,\n  nextIncidentSummaryExpiry,\n  reconcileIncidentAllScope,\n  toggleIncidentAllScope,\n  type IncidentFamilySource,\n  type MergedIncidentFamily,\n} from \"./admin.incidents.multisite\";\nimport type {\n  Family,\n  IncidentState,\n  IncidentType,\n} from \"../admin.management.v2\";\n\nconst typeLabels: Record<IncidentType, string> = {\n  producto_ausente: \"Producto ausente\",\n  codigo_interno_invalido: \"C.interno inválido\",\n  codigo_interno_duplicado: \"C.interno duplicado\",\n  stock_invalido: \"Stock inválido\",\n};\nconst stateLabels: Record<IncidentState, string> = {\n  pendiente: \"Pendiente\",\n  suprimida: \"Suprimida\",\n  resuelta: \"Resuelta\",\n};\nconst stateViews: Array<{ state: IncidentState; label: string }> = [\n  { state: \"pendiente\", label: \"Pendientes\" },\n  { state: \"suprimida\", label: \"Suprimidas\" },\n  { state: \"resuelta\", label: \"Resueltas\" },\n];\n\nfunction StateBadge({ state }: { state: IncidentState }) {\n  return (\n    <span\n      className={`admin-incidents__state admin-status-badge admin-status-badge--${state === \"pendiente\" ? \"warning\" : state === \"resuelta\" ? \"success\" : \"info\"} admin-incidents__state--${state}`}\n    >\n      {stateLabels[state]}\n    </span>\n  );\n}\n\nfunction FamilyDetail({\n  family,\n  site,\n  onClose,\n}: {\n  family: Family;\n  site?: string;\n  onClose: () => void;\n}) {\n  const [page, setPage] = useState(0);\n  const query = useManagementQuery(\"detail\", {\n    family_key: family.family_key,\n    ...(site ? { site_id: site } : {}),\n    page,\n    page_size: 100,\n  });\n  return (\n    <AdminDialog\n      title={`Repeticiones · ${typeLabels[family.tipo]}`}\n      description={\n        site\n          ? \"Detalle solicitado bajo demanda para una sede fuente.\"\n          : \"Detalle solicitado bajo demanda para todas las sedes.\"\n      }\n      onClose={onClose}\n      wide\n    >\n      {query.data ? (\n        <>\n          <div\n            className=\"admin-v2-table admin-incidents__detail-table\"\n            role=\"region\"\n            aria-label=\"Detalle de repeticiones\"\n            tabIndex={0}\n          >\n            <table>\n              <thead>\n                <tr>\n                  <th>Sede</th>\n                  <th>Estado</th>\n                  <th>Vigencia</th>\n                  <th>Primera detección</th>\n                  <th>Última detección</th>\n                  <th>Resuelta</th>\n                  <th>Apariciones</th>\n                  <th>Datos</th>\n                </tr>\n              </thead>\n              <tbody>\n                {query.data.items.map((item) => (\n                  <tr key={item.id}>\n                    <td>{item.sede}</td>\n                    <td>\n                      <StateBadge state={item.estado} />\n                    </td>\n                    <td>{item.active ? \"Vigente\" : \"Histórica\"}</td>\n                    <td>{incidentTimestamp(item.first_seen_at)}</td>\n                    <td>{incidentTimestamp(item.last_seen_at)}</td>\n                    <td>\n                      {item.resuelta_at\n                        ? incidentTimestamp(item.resuelta_at)\n                        : \"—\"}\n                    </td>\n                    <td>{item.occurrence_count}</td>\n                    <td>\n                      <details>\n                        <summary>Ver datos</summary>\n                        <pre className=\"admin-v2-json\">\n                          {JSON.stringify(item.datos, null, 2)}\n                        </pre>\n                      </details>\n                    </td>\n                  </tr>\n                ))}\n              </tbody>\n            </table>\n          </div>\n          <div className=\"admin-v2-toolbar\">\n            <button\n              type=\"button\"\n              className=\"button button--secondary\"\n              disabled={!page}\n              onClick={() => setPage(page - 1)}\n            >\n              <ChevronLeft size={16} aria-hidden=\"true\" />\n              Anterior\n            </button>\n            <span>Página {page + 1}</span>\n            <button\n              type=\"button\"\n              className=\"button button--secondary\"\n              disabled={query.data.items.length < 100}\n              onClick={() => setPage(page + 1)}\n            >\n              Siguiente\n              <ChevronRight size={16} aria-hidden=\"true\" />\n            </button>\n          </div>\n        </>\n      ) : (\n        <ReadNotice {...query} />\n      )}\n    </AdminDialog>\n  );\n}\n\nfunction canProposeDelete(family: Family) {\n  return (\n    family.tipo === \"producto_ausente\" &&\n    family.active &&\n    family.family_state === \"pendiente\" &&\n    family.c_interno !== null &&\n    Number.isSafeInteger(family.c_interno) &&\n    family.c_interno > 0 &&\n    !family.deletion_proposed\n  );\n}\n\nfunction globalIncidentRevision(\n  store: ReturnType<typeof useManagement>,\n  family: MergedIncidentFamily,\n) {\n  const revisions = [\n    ...new Set(\n      family.sources\n        .map(\n          (source) =>\n            store.peek(\"summary\", { site_id: source.siteId }).data?.revisions\n              .incidents_global,\n        )\n        .filter((value): value is number => typeof value === \"number\"),\n    ),\n  ];\n  return revisions.length === 1 ? revisions[0] : null;\n}\n\nfunction IgnoreDialog({\n  family,\n  onClose,\n  onSuccess,\n}: {\n  family: MergedIncidentFamily;\n  onClose: () => void;\n  onSuccess: () => void;\n}) {\n  const store = useManagement();\n  const [running, setRunning] = useState(false);\n  const [error, setError] = useState(\"\");\n  const retryable = store.intent(\"incidents\")?.action === \"ignore_30d\";\n  const ignore = () => {\n    const revision = globalIncidentRevision(store, family);\n    if (!retryable && revision === null) {\n      setError(\n        \"La revisión global de incidencias no está disponible. Actualiza la vista y reintenta.\",\n      );\n      return;\n    }\n    setError(\"\");\n    setRunning(true);\n    const request = retryable\n      ? store.retryMutation(\"incidents\")\n      : store.mutation(\n          \"ignore_30d\",\n          {\n            family_key: family.family_key,\n            scope: \"global\",\n          },\n          revision!,\n        );\n    void request\n      .then(() => {\n        onSuccess();\n        onClose();\n      })\n      .catch((reason) =>\n        setError(\n          reason instanceof Error\n            ? reason.message\n            : \"No se pudo ignorar la incidencia.\",\n        ),\n      )\n      .finally(() => setRunning(false));\n  };\n  return (\n    <AdminDialog\n      title=\"Ignorar incidencia durante 30 días\"\n      description={`${typeLabels[family.tipo]} · ${family.c_interno ?? family.c_interno_original ?? \"Sin código\"}`}\n      onClose={onClose}\n      closeDisabled={running}\n      footer={\n        <>\n          <button\n            type=\"button\"\n            className=\"button button--secondary\"\n            disabled={running}\n            onClick={onClose}\n          >\n            Cancelar\n          </button>\n          <button\n            type=\"button\"\n            className=\"button button--primary\"\n            disabled={running}\n            onClick={ignore}\n          >\n            <AlarmClockOff size={16} aria-hidden=\"true\" />\n            {running ? \"Procesando…\" : retryable ? \"Reintentar\" : \"Ignorar 30 días\"}\n          </button>\n        </>\n      }\n    >\n      <p>Esta incidencia se ignorará durante 30 días en todas las sedes.</p>\n      {error && <AdminNotice tone=\"error\">{error}</AdminNotice>}\n    </AdminDialog>\n  );\n}\n\ntype DeleteProposal = {\n  family: MergedIncidentFamily;\n  source: IncidentFamilySource;\n};\n\nfunction DeleteProposalDialog({\n  proposal,\n  onClose,\n  onConfirm,\n  onRetry,\n  retryable,\n}: {\n  proposal: DeleteProposal;\n  onClose: () => void;\n  onConfirm: () => Promise<void>;\n  onRetry: () => Promise<void>;\n  retryable: boolean;\n}) {\n  const [proposing, setProposing] = useState(false);\n  const [error, setError] = useState(\"\");\n  const product =\n    typeof proposal.source.family.datos.producto === \"string\"\n      ? proposal.source.family.datos.producto\n      : null;\n  const confirm = () => {\n    if (proposing) return;\n    setError(\"\");\n    setProposing(true);\n    void (retryable ? onRetry() : onConfirm())\n      .then(onClose)\n      .catch((reason) =>\n        setError(\n          reason instanceof Error\n            ? reason.message\n            : \"No se pudo proponer la eliminación.\",\n        ),\n      )\n      .finally(() => setProposing(false));\n  };\n  return (\n    <AdminDialog\n      title=\"Proponer eliminación\"\n      description=\"Se creará una propuesta de eliminación para revisión en Catálogo.\"\n      onClose={onClose}\n      closeDisabled={proposing}\n      footer={\n        <>\n          <button\n            type=\"button\"\n            className=\"button button--secondary\"\n            disabled={proposing}\n            onClick={onClose}\n          >\n            Cancelar\n          </button>\n          <button\n            type=\"button\"\n            className=\"button button--danger\"\n            disabled={proposing}\n            onClick={confirm}\n          >\n            {proposing ? \"Procesando…\" : retryable ? \"Reintentar\" : \"Proponer eliminación\"}\n          </button>\n        </>\n      }\n    >\n      <p>\n        <strong>\n          Producto ausente ·{\" \"}\n          {proposal.source.family.c_interno ??\n            proposal.source.family.c_interno_original ??\n            \"Sin código\"}\n        </strong>\n      </p>\n      {product && <p>{product}</p>}\n      <p>\n        Detectado en: <strong>{proposal.source.siteName}</strong>\n      </p>\n      {error && <AdminNotice tone=\"error\">{error}</AdminNotice>}\n    </AdminDialog>\n  );\n}\nexport function AdminIncidentsV2() {\n  const admin = useAdminStore();\n  const store = useManagement();\n  const [type, setType] = useState<\"all\" | IncidentType>(\"all\");\n  const [state, setState] = useState<IncidentState>(\"pendiente\");\n  const [detailFamily, setDetailFamily] = useState<MergedIncidentFamily | null>(\n    null,\n  );\n  const [ignoreFamily, setIgnoreFamily] = useState<MergedIncidentFamily | null>(\n    null,\n  );\n  const [deleteProposal, setDeleteProposal] = useState<DeleteProposal | null>(\n    null,\n  );\n  const [allOriginSite, setAllOriginSite] = useState<string | null>(null);\n  const [allLoading, setAllLoading] = useState(false);\n  const [error, setErrorState] = useState(\"\");\n  const [notice, setNoticeState] = useState(\"\");\n  const [feedbackOccurrence, setFeedbackOccurrence] = useState(0);\n  const [dismissedNotice, setDismissedNotice] = useState(\"\");\n  const setError = (message: string) => {\n    setErrorState(message);\n    if (message) setFeedbackOccurrence((current) => current + 1);\n  };\n  const setNotice = (message: string) => {\n    setNoticeState(message);\n    if (message) setFeedbackOccurrence((current) => current + 1);\n  };\n  const allLoadRef = useRef<Promise<void> | null>(null);\n  const [allSnapshot, setAllSnapshot] = useState<MergedIncidentFamily[]>([]);\n  const siteId = admin.siteId;\n  const allActive = incidentAllScopeActive(allOriginSite, siteId);\n  const normalQuery = useManagementQuery(\n    \"summary\",\n    { site_id: siteId },\n    { enabled: !!siteId && !allActive },\n  );\n  const sites = useMemo(\n    () => orderedAdminSites(admin.bootstrap?.allowed_sites ?? []),\n    [admin.bootstrap],\n  );\n  const ensureAllSummaries = useCallback(() => {\n    if (allLoadRef.current) return allLoadRef.current;\n    setAllLoading(true);\n    const request = loadIncidentSummaries(store, sites).then(() => undefined);\n    allLoadRef.current = request;\n    const cleanup = () => {\n      if (allLoadRef.current === request) allLoadRef.current = null;\n      setAllLoading(false);\n    };\n    void request.then(cleanup, cleanup);\n    return request;\n  }, [sites, store]);\n\n  useEffect(() => {\n    const reconciled = reconcileIncidentAllScope(allOriginSite, siteId);\n    if (reconciled === allOriginSite) return;\n    const cancel = window.setTimeout(() => setAllOriginSite(reconciled), 0);\n    return () => window.clearTimeout(cancel);\n  }, [allOriginSite, siteId]);\n\n  const allSources = sites\n    .map((site) => {\n      const summary = store.peek(\"summary\", { site_id: site.id }).data;\n      return summary\n        ? { siteId: site.id, siteName: site.nombre, summary }\n        : null;\n    })\n    .filter((value): value is NonNullable<typeof value> => value !== null);\n  const allComplete = sites.length > 0 && allSources.length === sites.length;\n  const mergedAll = useMemo(\n    () => (allComplete ? mergeIncidentSummaries(allSources) : null),\n    [allComplete, allSources],\n  );\n  useEffect(() => {\n    if (!allActive || !mergedAll) return;\n    const update = window.setTimeout(() => setAllSnapshot(mergedAll), 0);\n    return () => window.clearTimeout(update);\n  }, [allActive, mergedAll]);\n\n  const allNextExpiry = nextIncidentSummaryExpiry(store, sites);\n  useEffect(() => {\n    if (!allActive || !sites.length) return;\n\n    const refreshIfNeeded = () => {\n      const missing = sites.some(\n        (site) => !store.peek(\"summary\", { site_id: site.id }).data,\n      );\n      if (!missing) return;\n      setError(\"\");\n      setNotice(\"\");\n      void ensureAllSummaries().catch((reason) => {\n        setAllOriginSite(null);\n        setError(\n          reason instanceof Error\n            ? reason.message\n            : \"No se pudo actualizar todas las sedes.\",\n        );\n      });\n    };\n\n    if (!allComplete) {\n      refreshIfNeeded();\n      return;\n    }\n\n    const delay =\n      allNextExpiry === undefined\n        ? null\n        : Math.max(0, allNextExpiry - Date.now()) + 25;\n    const timer =\n      delay === null ? null : window.setTimeout(refreshIfNeeded, delay);\n    const onPageShow = () => refreshIfNeeded();\n    const onVisibility = () => {\n      if (document.visibilityState === \"visible\") refreshIfNeeded();\n    };\n    window.addEventListener(\"pageshow\", onPageShow);\n    document.addEventListener(\"visibilitychange\", onVisibility);\n    return () => {\n      if (timer !== null) window.clearTimeout(timer);\n      window.removeEventListener(\"pageshow\", onPageShow);\n      document.removeEventListener(\"visibilitychange\", onVisibility);\n    };\n  }, [allActive, allComplete, allNextExpiry, ensureAllSummaries, sites, store]);\n\n  const displayedFamilies = useMemo(() => {\n    if (allActive) return mergedAll ?? allSnapshot;\n    if (!normalQuery.data || !siteId) return [];\n    const siteName = sites.find((site) => site.id === siteId)?.nombre ?? siteId;\n    return mergeIncidentSummaries([\n      { siteId, siteName, summary: normalQuery.data },\n    ]);\n  }, [allActive, allSnapshot, mergedAll, normalQuery.data, siteId, sites]);\n  const families = useMemo(\n    () =>\n      displayedFamilies.filter(\n        (item) =>\n          (type === \"all\" || item.tipo === type) && item.family_state === state,\n      ),\n    [displayedFamilies, state, type],\n  );\n  const stateCounts = useMemo(() => {\n    const available = displayedFamilies.filter(\n      (item) => type === \"all\" || item.tipo === type,\n    );\n    return {\n      pendiente: available.filter((item) => item.family_state === \"pendiente\")\n        .length,\n      suprimida: available.filter((item) => item.family_state === \"suprimida\")\n        .length,\n      resuelta: available.filter((item) => item.family_state === \"resuelta\")\n        .length,\n    };\n  }, [displayedFamilies, type]);\n  const toggleAll = async () => {\n    const nextOrigin = toggleIncidentAllScope(allOriginSite, siteId);\n    if (nextOrigin === null) {\n      setAllOriginSite(null);\n      return;\n    }\n    setError(\"\");\n    setNotice(\"\");\n    try {\n      await ensureAllSummaries();\n      setAllOriginSite(nextOrigin);\n    } catch (reason) {\n      setError(\n        reason instanceof Error\n          ? reason.message\n          : \"No se pudo cargar todas las sedes.\",\n      );\n    }\n  };\n  const reactivateFamily = (family: MergedIncidentFamily): Promise<void> => {\n    const revision = globalIncidentRevision(store, family);\n    if (revision === null) {\n      const reason = new Error(\n        \"La revisión global de incidencias no está disponible. Actualiza la vista y reintenta.\",\n      );\n      setError(reason.message);\n      return Promise.reject(reason);\n    }\n    setError(\"\");\n    setNotice(\"\");\n    return store\n      .mutation(\n        \"reactivate\",\n        {\n          family_key: family.family_key,\n          scope: \"global\",\n        },\n        revision,\n      )\n      .then(() => {\n        setAllSnapshot([]);\n        setNotice(\"La incidencia fue reactivada.\");\n      })\n      .catch((reason) => {\n        if (!store.intent(\"incidents\")) {\n          setError(\n            reason instanceof Error\n              ? reason.message\n              : \"No se pudo actualizar la incidencia.\",\n          );\n        }\n        throw reason;\n      });\n  };\n  const proposeDeleteSource = (\n    source: IncidentFamilySource,\n  ): Promise<void> => {\n    const summary = store.peek(\"summary\", { site_id: source.siteId }).data;\n    if (!summary) {\n      return Promise.reject(\n        new Error(\"La sede ya no tiene un summary vigente. Reintenta la acción.\"),\n      );\n    }\n    setError(\"\");\n    setNotice(\"\");\n    return store\n      .mutation(\n        \"propose_delete\",\n        {\n          family_key: source.family.family_key,\n          scope: \"site\",\n          site_id: source.siteId,\n        },\n        summary.revisions.incidents,\n        source.siteId,\n      )\n      .then(() => {\n        setNotice(\n          \"La propuesta de eliminación quedó pendiente para revisión en Catálogo.\",\n        );\n      })\n      .catch((reason) => {\n        throw reason;\n      });\n  };\n  const pendingIntent = store.intent(\"incidents\");\n  const pending = !!pendingIntent;\n  const retryIncidentIntent = (): Promise<void> => {\n    const intent = store.intent(\"incidents\");\n    if (!intent) return Promise.reject(new Error(\"No hay una operación pendiente.\"));\n    setError(\"\");\n    setNotice(\"\");\n    return store.retryMutation(\"incidents\").then(() => {\n      setAllSnapshot([]);\n      const success =\n        intent.action === \"ignore_30d\"\n          ? \"La incidencia fue ignorada durante 30 días.\"\n          : intent.action === \"reactivate\"\n            ? \"La incidencia fue reactivada.\"\n            : \"La propuesta de eliminación quedó pendiente para revisión en Catálogo.\";\n      setNotice(success);\n    }).catch((reason) => {\n      if (!store.intent(\"incidents\")) {\n        setError(\n          reason instanceof Error\n            ? reason.message\n            : \"No se pudo actualizar la incidencia.\",\n        );\n      }\n      throw reason;\n    });\n  };\n  const noticeMessage = error || pendingIntent?.error || notice;\n  const noticeKey = pendingIntent\n    ? `intent:${String(pendingIntent.payload.operation_id)}:${pendingIntent.attempt}:${pendingIntent.error ? \"error\" : \"pending\"}`\n    : `local:${feedbackOccurrence}`;\n  const hasData = allActive || !!normalQuery.data;\n  return (\n    <section className=\"admin-incidents\">\n      <div className=\"admin-incidents__controls\">\n        <div\n          className=\"admin-state-views\"\n          role=\"tablist\"\n          aria-label=\"Estado de incidencias\"\n          onKeyDown={(event) => {\n            if (\n              ![\n                \"ArrowRight\",\n                \"ArrowDown\",\n                \"ArrowLeft\",\n                \"ArrowUp\",\n                \"Home\",\n                \"End\",\n              ].includes(event.key)\n            )\n              return;\n            event.preventDefault();\n            const currentIndex = stateViews.findIndex(\n              (view) => view.state === state,\n            );\n            const direction =\n              event.key === \"ArrowRight\" || event.key === \"ArrowDown\" ? 1 : -1;\n            const nextIndex =\n              event.key === \"Home\"\n                ? 0\n                : event.key === \"End\"\n                  ? stateViews.length - 1\n                  : (currentIndex + direction + stateViews.length) %\n                    stateViews.length;\n            const nextView = stateViews[nextIndex];\n            setState(nextView.state);\n            event.currentTarget\n              .querySelector<HTMLButtonElement>(\n                `#admin-incidents-state-${nextView.state}`,\n              )\n              ?.focus();\n          }}\n        >\n          {stateViews.map((view) => (\n            <button\n              key={view.state}\n              id={`admin-incidents-state-${view.state}`}\n              type=\"button\"\n              role=\"tab\"\n              aria-selected={state === view.state}\n              aria-controls=\"admin-incidents-state-panel\"\n              tabIndex={state === view.state ? 0 : -1}\n              onClick={() => setState(view.state)}\n            >\n              <span>{view.label}</span>\n              <strong>{stateCounts[view.state]}</strong>\n            </button>\n          ))}\n        </div>\n        <label className=\"admin-toolbar__filter admin-incidents__type\">\n          Tipo:\n          <select\n            value={type}\n            onChange={(event) =>\n              setType(event.target.value as \"all\" | IncidentType)\n            }\n          >\n            <option value=\"all\">Todos</option>\n            {Object.entries(typeLabels).map(([value, label]) => (\n              <option key={value} value={value}>\n                {label}\n              </option>\n            ))}\n          </select>\n        </label>\n        <button\n          type=\"button\"\n          className=\"button button--primary\"\n          aria-pressed={allActive}\n          disabled={allLoading || !sites.length}\n          onClick={() => void toggleAll()}\n        >\n          {allLoading ? \"Cargando sedes…\" : \"Todas las sedes\"}\n        </button>\n      </div>\n      {noticeMessage && dismissedNotice !== noticeKey && (\n        <AdminNotice\n          tone={error || pendingIntent?.error ? \"error\" : \"success\"}\n          onDismiss={pendingIntent ? undefined : () => setDismissedNotice(noticeKey)}\n          action={\n            pendingIntent && !pendingIntent.pending ? (\n              <button\n                type=\"button\"\n                className=\"button button--secondary\"\n                onClick={() => void retryIncidentIntent().catch(() => {})}\n              >\n                Reintentar\n              </button>\n            ) : undefined\n          }\n        >\n          {error || pendingIntent?.error || notice}\n        </AdminNotice>\n      )}{\" \"}\n      {hasData ? (\n        <div className=\"admin-table-section admin__table\">\n          <div\n            className=\"admin-v2-table\"\n            role=\"tabpanel\"\n            id=\"admin-incidents-state-panel\"\n            aria-labelledby={`admin-incidents-state-${state}`}\n            tabIndex={0}\n          >\n            <table>\n              <thead>\n                <tr>\n                  <th>Tipo</th>\n                  <th>Producto</th>\n                  <th>Sedes</th>\n                  <th>\n                    {state === \"pendiente\"\n                      ? \"Última detección\"\n                      : state === \"suprimida\"\n                        ? \"Ignorada hasta\"\n                        : \"Resuelta\"}\n                  </th>\n                  <th>Acciones</th>\n                </tr>\n              </thead>\n              <tbody>\n                {families.map((item) => {\n                  const product =\n                    typeof item.datos.producto === \"string\" &&\n                    item.datos.producto.trim()\n                      ? item.datos.producto\n                      : \"Producto sin nombre\";\n                  const date =\n                    state === \"pendiente\"\n                      ? item.last_seen_at\n                      : state === \"suprimida\"\n                        ? item.active_suppression_until\n                        : item.resolved_at;\n                  const reactivable = item.active_suppression_until !== null;\n                  const deletable = item.sources.find((source) =>\n                    canProposeDelete(source.family),\n                  );\n                  return (\n                    <tr key={item.family_key}>\n                      <td>\n                        <span className=\"admin-attribute-badge admin-incidents__type-badge\">\n                          {typeLabels[item.tipo]}\n                        </span>\n                      </td>\n                      <th scope=\"row\" className=\"admin-incidents__product\">\n                        <span>{product}</span>\n                        <small>\n                          {item.c_interno ??\n                            item.c_interno_original ??\n                            \"Sin código\"}\n                        </small>\n                      </th>\n                      <td>\n                        <div className=\"admin-incidents__sites\">\n                          {item.sources.map((source) => (\n                            <span\n                              key={source.siteId}\n                              className=\"admin-attribute-badge\"\n                            >\n                              {incidentSiteAbbreviation(source.siteName)}\n                            </span>\n                          ))}\n                        </div>\n                      </td>\n                      <td>{incidentTimestamp(date)}</td>\n                      <td>\n                        <div className=\"admin-v2-actions\">\n                          <button\n                            type=\"button\"\n                            className=\"icon-button\"\n                            aria-label={`Ver repeticiones ${item.family_key}`}\n                            title=\"Ver detalle\"\n                            onClick={() => setDetailFamily(item)}\n                          >\n                            <Eye size={16} aria-hidden=\"true\" />\n                          </button>\n                          {state === \"pendiente\" &&\n                            item.active &&\n                            !item.sources.every(\n                              (source) => source.family.reactivate_available,\n                            ) && (\n                              <button\n                                type=\"button\"\n                                className=\"icon-button icon-button--warning\"\n                                aria-label=\"Ignorar 30 días\"\n                                title=\"Ignorar 30 días\"\n                                disabled={pending}\n                                onClick={() => setIgnoreFamily(item)}\n                              >\n                                <AlarmClockOff size={16} aria-hidden=\"true\" />\n                              </button>\n                            )}\n                          {state === \"pendiente\" && deletable && (\n                            <button\n                              type=\"button\"\n                              className=\"icon-button icon-button--danger\"\n                              aria-label=\"Proponer eliminación\"\n                              title=\"Proponer eliminación\"\n                              disabled={pending}\n                              onClick={() =>\n                                setDeleteProposal({\n                                  family: item,\n                                  source: deletable,\n                                })\n                              }\n                            >\n                              <CircleOff size={16} aria-hidden=\"true\" />\n                            </button>\n                          )}\n                          {state === \"suprimida\" && reactivable && (\n                            <button\n                              type=\"button\"\n                              className=\"icon-button\"\n                              aria-label=\"Reactivar incidencia\"\n                              title=\"Reactivar incidencia\"\n                              disabled={pending}\n                              onClick={() =>\n                                void reactivateFamily(item).catch(() => {})\n                              }\n                            >\n                              <RotateCcw size={16} aria-hidden=\"true\" />\n                            </button>\n                          )}\n                        </div>\n                      </td>\n                    </tr>\n                  );\n                })}{\" \"}\n                {!families.length && (\n                  <tr>\n                    <td colSpan={5}>\n                      No hay incidencias que coincidan con los filtros locales.\n                    </td>\n                  </tr>\n                )}\n              </tbody>\n            </table>\n          </div>\n        </div>\n      ) : (\n        <ReadNotice {...normalQuery} />\n      )}\n      {detailFamily && (\n        <FamilyDetail\n          key={`${allActive ? \"all\" : siteId}:${detailFamily.family_key}`}\n          family={detailFamily}\n          site={allActive ? undefined : siteId}\n          onClose={() => setDetailFamily(null)}\n        />\n      )}\n      {ignoreFamily && (\n        <IgnoreDialog\n          family={ignoreFamily}\n          onClose={() => setIgnoreFamily(null)}\n          onSuccess={() => {\n            setAllSnapshot([]);\n            setNotice(\"La incidencia fue ignorada durante 30 días.\");\n          }}\n        />\n      )}\n      {deleteProposal && (\n        <DeleteProposalDialog\n          proposal={deleteProposal}\n          onClose={() => setDeleteProposal(null)}\n          onConfirm={() => proposeDeleteSource(deleteProposal.source)}\n          onRetry={retryIncidentIntent}\n          retryable={pendingIntent?.action === \"propose_delete\"}\n        />\n      )}\n    </section>\n  );\n}\n"

      at <anonymous> (/home/runner/work/SOLOG/SOLOG/tests/admin-incidents-multisite.test.ts:205:20)

::error file=tests/admin-incidents-multisite.test.ts,line=205,col=20,title=error: expect(received).toMatch(expected)::Expected substring or pattern: /void onConfirm\(\)\s*\.then\(onClose\)\s*\.catch/%0Aas.\");\n          }}\n        />\n      )}\n      {deleteProposal && (\n        <DeleteProposalDialog\n          proposal={deleteProposal}\n          onClose={() => setDeleteProposal(null)}\n          onConfirm={() => proposeDeleteSource(deleteProposal.source)}\n          onRetry={retryIncidentIntent}\n          retryable={pendingIntent?.action === \"propose_delete\"}\n        />\n      )}\n    </section>\n  );\n}\n"%0A%0A      at <anonymous> (/home/runner/work/SOLOG/SOLOG/tests/admin-incidents-multisite.test.ts:205:20)
(fail) Incidencias multisede > CircleOff difiere propose_delete hasta la confirmación con la fuente seleccionada [1.02ms]
(pass) Incidencias multisede > la fecha de supresión multisede solo se conserva cuando las fuentes coinciden [0.25ms]
(pass) Incidencias multisede > Ignorar y Reactivar usan revisión global y el detalle multisede omite site_id [0.28ms]

::endgroup::

::group::tests/admin-catalog-v3.store.test.ts:
(pass) store Catálogo V3 > deduplica y conserva conjuntos completos por acción y payload [0.74ms]
(pass) store Catálogo V3 > una respuesta con revisión nueva invalida conjuntos anteriores, sin usar bootstrap como revisión [0.17ms]
(pass) store Catálogo V3 > rechaza una lectura con revisión anterior a la fuente V3 [0.14ms]
(pass) store Catálogo V3 > conserva operation_id y ambas revisiones en reintentos inciertos [0.58ms]
(pass) store Catálogo V3 > retirar aprobación invalida y obliga a releer el estado autoritativo [0.25ms]
(pass) store Catálogo V3 > toda mutación exitosa invalida caché aunque las revisiones no cambien [0.14ms]
(pass) store Catálogo V3 > proponer estado de Producto solo crea una propuesta e invalida el conjunto completo [0.13ms]
(pass) store Catálogo V3 > prepara onboarding en staging para grupo existente y grupo unitario nuevo [0.16ms]
(pass) store Catálogo V3 > prepara resolución de precio con decisión explícita xN y sin cálculo proporcional [0.20ms]
(pass) store Catálogo V3 > publica solo como admin y conserva el mismo operation_id después de una respuesta incierta [0.41ms]
(pass) store Catálogo V3 > mantiene el recibo cuando el commit se confirmó pero falta registrar el cierre [0.13ms]
(pass) store Catálogo V3 > un conflicto definitivo descarta intención e invalida caché [0.12ms]
(pass) store Catálogo V3 > una respuesta pendiente no sobrevive un cambio de rol [0.16ms]

::endgroup::

::group::tests/admin-shell-devices.test.ts:
(pass) Shell orders sites and aliases Casuarinas without changing IDs or input [0.22ms]
(pass) Shell site selection is UI-only, retained across module loads, and scoped to this store [1.76ms]
(pass) Shell does not accept unauthorized sites and falls back if access changes [0.27ms]
(pass) Device last access uses Lima today, 12-hour time and explicit missing access [3.32ms]
(pass) Shell selection emits only on a valid change and never invalidates cached datasets [0.33ms]

::endgroup::

::group::tests/admin-products-masterdata.test.ts:
(pass) Admin Productos con Master Data > registra la ruta Productos y la ubica en Inventario entre Catálogo y Grupos [0.23ms]
(pass) Admin Productos con Master Data > filtra y ordena el conjunto completo local usando relaciones derivadas [0.48ms]
(pass) Admin Productos con Master Data > pagina localmente sin alterar ni truncar el dataset compartido [0.14ms]
(pass) Admin Productos con Master Data > carga Master Data solo desde Productos o el diálogo de configuración [0.40ms]
(pass) Admin Productos con Master Data > onboarding conserva prepare_product y no vuelve a Catalog reference [0.16ms]
(pass) Admin Productos con Master Data > QuickFilterChip muestra contadores derivados sin lectura adicional y conserva filtros restantes [0.34ms]

::endgroup::

::group::tests/admin-management.test.ts:
(pass) A4–A6 reads and cache > status preserves its exact response [0.35ms]
(pass) A4–A6 reads and cache > reference preserves its exact response [0.03ms]
(pass) A4–A6 reads and cache > groups preserves its exact response [0.02ms]
(pass) A4–A6 reads and cache > group_products preserves its exact response [0.04ms]
(pass) A4–A6 reads and cache > catalog_changes preserves its exact response [0.01ms]
(pass) A4–A6 reads and cache > publication_preview preserves its exact response [0.01ms]
(pass) A4–A6 reads and cache > price_mismatch_options preserves its exact response [0.01ms]
(pass) A4–A6 reads and cache > summary preserves its exact response [0.19ms]
(pass) A4–A6 reads and cache > detail preserves its exact response [0.11ms]
(pass) A4–A6 reads and cache > list preserves its exact response [0.19ms]
(pass) A4–A6 reads and cache > summary y detail exigen el contrato operativo V2 y la reactivación por scope [0.32ms]
(pass) A4–A6 reads and cache > master limit/offset; no generic data; device list has row revisions only [0.18ms]
(pass) A4–A6 reads and cache > deduplicates, caches and separates sites; blocks unauthorized site [0.44ms]
(pass) A4–A6 reads and cache > discards in-flight reads after refresh and disposal [0.27ms]
(pass) A4–A6 reads and cache > rejects mismatched family, page, site and stale revisions [0.38ms]
(pass) A4–A6 mutation intentions > group_change_save success and lost-response replay retain UUID/payload [0.38ms]
(pass) A4–A6 mutation intentions > catalog_change_action success and lost-response replay retain UUID/payload [0.10ms]
(pass) A4–A6 mutation intentions > resolve_group_price success and lost-response replay retain UUID/payload [0.07ms]
(pass) A4–A6 mutation intentions > update_package_price success and lost-response replay retain UUID/payload [0.04ms]
(pass) A4–A6 mutation intentions > ignore_30d success and lost-response replay retain UUID/payload [0.09ms]
(pass) A4–A6 mutation intentions > reactivate success and lost-response replay retain UUID/payload [0.12ms]
(pass) A4–A6 mutation intentions > propose_delete success and lost-response replay retain UUID/payload [0.06ms]
(pass) A4–A6 mutation intentions > authorize success and lost-response replay retain UUID/payload [0.07ms]
(pass) A4–A6 mutation intentions > replace success and lost-response replay retain UUID/payload [0.08ms]
(pass) A4–A6 mutation intentions > revoke success and lost-response replay retain UUID/payload [0.08ms]
(pass) A4–A6 mutation intentions > reject success and lost-response replay retain UUID/payload [0.07ms]
(pass) A4–A6 mutation intentions > revision conflict releases rejected intent, invalidates and uses a new UUID [0.53ms]
(pass) A4–A6 mutation intentions > device mutation invalidates target/global list, preserves other site and master [0.27ms]
(pass) A4–A6 mutation intentions > propose deletion invalidates catalog V3 and affected family, not devices [0.31ms]
(pass) A4–A6 mutation intentions > old replay never rolls revisions back [0.24ms]
(pass) A4–A6 mutation intentions > prepared publication retry preserves operation; no client artifact fields [0.32ms]
(pass) A4–A6 mutation intentions > moderator cannot publish; disposed scope cannot mutate [0.09ms]

::endgroup::

::group::tests/admin-catalog-v3.structure.test.ts:
(pass) Catálogo V3: estructura principal > las rutas cargan Catálogo y Productos como módulos lazy separados [0.43ms]
(pass) Catálogo V3: estructura principal > Catálogo conserva Propuestas y retira la pestaña y lectura Productos [0.33ms]
(pass) Catálogo V3: estructura principal > consume los cuatro estados y conserva la clasificación y acciones de Propuestas V3 [0.60ms]
45 |     expect(ui).toContain('store.publish()')
46 |   })
47 | 
48 |   test('expone estados UI, reintentos y controles accesibles de staging y publicación', async () => {
49 |     const ui = await source('src/features/solog/admin/catalogo/admin.catalogo.page.v3.tsx')
50 |     for (const state of ['QueryState', 'role="alert"', 'role="status"', 'Reintentar misma operación', 'No hay propuestas', 'Existe staging preparado', 'No publicable:', 'Confirmar publicación', 'Recuperar publicación']) expect(ui).toContain(state)
                                                                                                                                                                                                                                           ^
error: expect(received).toContain(expected)

Expected to contain: "Reintentar misma operación"
Received: "import { useState } from \"react\";\nimport {\n  Check,\n  CircleOff,\n  ClipboardCheck,\n  DollarSign,\n  Eye,\n  EyeOff,\n  Package,\n  RefreshCw,\n  RotateCcw,\n  Settings,\n  Undo2,\n  Upload,\n  Wrench,\n} from \"lucide-react\";\nimport { AdminDialog } from \"../admin.dialog\";\nimport {\n  ValuationDialog,\n  type ValuationDecision,\n} from \"../admin.valuation-dialog\";\nimport { useAdminStore } from \"../admin.v2.context\";\nimport {\n  ProductSetupDialog,\n  type ProductSetupTarget,\n} from \"../productos/admin.product-setup.dialog\";\nimport { useCatalogQuery, useCatalogStore } from \"./admin.catalogo.context\";\nimport { CatalogMutationNotice, catalogMutationError } from \"./admin.catalogo.feedback\";\nimport {\n  catalogProposalChange,\n  type CatalogProposal,\n  type CatalogProposalStatus,\n} from \"./admin.catalogo.v3\";\nimport { adminTimestamp } from \"../admin.v2.format\";\nimport { QueryState, Value } from \"../admin.v2.presentation\";\nimport { AdminNotice } from \"../admin.primitives\";\n\ntype ProposalAction = \"approve\" | \"ignore\" | \"withdraw\";\ntype ProposalSection = \"urgent\" | \"emerging\";\ntype PriceResolution = \"update_group_price\" | \"separate_sku\" | \"keep_structure\";\n\nconst proposalStatuses: Array<{ id: CatalogProposalStatus; label: string }> = [\n  { id: \"pendiente\", label: \"Pendientes\" },\n  { id: \"aprobado\", label: \"Aprobados\" },\n  { id: \"ignorado\", label: \"Ignorados\" },\n  { id: \"incorporado\", label: \"Publicados\" },\n];\nconst urgentTypes = new Set<CatalogProposal[\"tipo\"]>([\n  \"agregar_producto\",\n  \"precio\",\n  \"reincorporar_producto\",\n]);\nconst emergingTypes = new Set<CatalogProposal[\"tipo\"]>([\n  \"eliminar_producto\",\n  \"excluir_producto\",\n  \"nombre\",\n  \"codigo\",\n]);\nconst proposalLabels: Record<CatalogProposal[\"tipo\"], string> = {\n  agregar_producto: \"Agregar producto\",\n  eliminar_producto: \"Eliminar producto\",\n  excluir_producto: \"Excluir producto\",\n  reincorporar_producto: \"Reincorporar producto\",\n  nombre: \"Cambiar nombre\",\n  codigo: \"Cambiar código de barras\",\n  precio: \"Cambiar precio\",\n};\nconst resolutionLabels: Record<PriceResolution, string> = {\n  update_group_price: \"Actualizar precio de todo el grupo\",\n  separate_sku: \"Separar SKU como Único\",\n  keep_structure: \"Conservar estructura del grupo\",\n};\nfunction priceErrorMessage(reason: unknown) {\n  const code =\n    reason && typeof reason === \"object\" && \"code\" in reason\n      ? String(reason.code)\n      : \"\";\n  if (\n    code === \"INVALID_PACKAGE_CONFIGURATION\" ||\n    code === \"SOLOG_INVALID_PACKAGE_CONFIGURATION\"\n  )\n    return \"La configuración de valorizado no es válida.\";\n  if (\n    code === \"INVALID_PACKAGE_PRICE\" ||\n    code === \"SOLOG_INVALID_PACKAGE_PRICE\"\n  )\n    return \"El precio por paquete no es válido.\";\n  if (\n    code === \"PACKAGE_PRICE_DECISION_REQUIRED\" ||\n    code === \"SOLOG_PACKAGE_PRICE_DECISION_REQUIRED\"\n  )\n    return \"Debes decidir explícitamente el valorizado antes de preparar.\";\n  return reason instanceof Error\n    ? reason.message\n    : \"No se pudo preparar el precio.\";\n}\n\nfunction CatalogStatus() {\n  const status = useCatalogQuery(\"status\", {});\n  if (!status.data) return <QueryState {...status} />;\n  const catalog = status.data.catalog;\n  return (\n    <p>\n      Versión {catalog.version_actual ?? \"sin publicar\"} ·{\" \"}\n      {adminTimestamp(catalog.publicado_at)} · {catalog.total} SKU\n    </p>\n  );\n}\nfunction classifyProposal(proposal: CatalogProposal): ProposalSection {\n  if (urgentTypes.has(proposal.tipo)) return \"urgent\";\n  if (emergingTypes.has(proposal.tipo)) return \"emerging\";\n  throw new Error(\"Tipo de propuesta Catálogo V3 no clasificable.\");\n}\n\nfunction ProposalsSurface() {\n  const [status, setStatus] = useState<CatalogProposalStatus>(\"pendiente\");\n  const [selected, setSelected] = useState<CatalogProposal | null>(null);\n  const query = useCatalogQuery(\"proposals\", { estado: status });\n  const rows = query.data?.rows ?? [];\n  const urgent = rows.filter(\n    (proposal) => classifyProposal(proposal) === \"urgent\",\n  );\n  const emerging = rows.filter(\n    (proposal) => classifyProposal(proposal) === \"emerging\",\n  );\n  return (\n    <>\n      <div\n        className=\"admin-state-views\"\n        role=\"tablist\"\n        aria-label=\"Estado de propuestas\"\n        onKeyDown={(event) => {\n          if (![\"ArrowLeft\", \"ArrowRight\", \"Home\", \"End\"].includes(event.key))\n            return;\n          event.preventDefault();\n          const current = proposalStatuses.findIndex(\n            (item) => item.id === status,\n          );\n          const next =\n            event.key === \"Home\"\n              ? 0\n              : event.key === \"End\"\n                ? proposalStatuses.length - 1\n                : (current +\n                    (event.key === \"ArrowRight\" ? 1 : -1) +\n                    proposalStatuses.length) %\n                  proposalStatuses.length;\n          setStatus(proposalStatuses[next].id);\n          const tabs =\n            event.currentTarget.querySelectorAll<HTMLButtonElement>(\n              '[role=\"tab\"]',\n            );\n          tabs.item(next)?.focus();\n        }}\n      >\n        {proposalStatuses.map((item) => (\n          <button\n            type=\"button\"\n            role=\"tab\"\n            id={`admin-catalog-state-${item.id}`}\n            key={item.id}\n            aria-selected={status === item.id}\n            aria-controls=\"admin-catalog-proposals-panel\"\n            tabIndex={status === item.id ? 0 : -1}\n            onClick={() => setStatus(item.id)}\n          >\n            {item.label}\n            {query.data && <strong>{query.data.counts[item.id]}</strong>}\n          </button>\n        ))}\n      </div>\n      <div\n        id=\"admin-catalog-proposals-panel\"\n        role=\"tabpanel\"\n        aria-labelledby={`admin-catalog-state-${status}`}\n      >\n        {!query.data ? (\n          <QueryState {...query} />\n        ) : (\n          <div className=\"admin-catalog__pending\">\n            <ProposalSection\n              title=\"Urgentes\"\n              rows={urgent}\n              section=\"urgent\"\n              onSelect={setSelected}\n            />\n            <ProposalSection\n              title=\"Emergentes\"\n              rows={emerging}\n              section=\"emerging\"\n              onSelect={setSelected}\n            />\n          </div>\n        )}\n      </div>\n      {selected && (\n        <ProposalDetail proposal={selected} onClose={() => setSelected(null)} />\n      )}\n    </>\n  );\n}\nfunction ProposalSection({\n  title,\n  rows,\n  section,\n  onSelect,\n}: {\n  title: string;\n  rows: CatalogProposal[];\n  section: ProposalSection;\n  onSelect: (proposal: CatalogProposal) => void;\n}) {\n  return (\n    <section\n      className={`admin-catalog__section admin-catalog__section--${section}`}\n    >\n      <header>\n        <h3>{title}</h3>\n        <span>{rows.length}</span>\n      </header>\n      <div className=\"admin-v2-table admin-catalog__table\">\n        <table>\n          <thead>\n            <tr>\n              <th scope=\"col\">Tipo</th>\n              <th scope=\"col\">Producto</th>\n              <th scope=\"col\">Cambio</th>\n              <th scope=\"col\">Origen</th>\n              <th scope=\"col\">Acción</th>\n            </tr>\n          </thead>\n          <tbody>\n            {rows.map((proposal) => (\n              <ProposalRow\n                key={proposal.propuesta_fingerprint}\n                proposal={proposal}\n                onSelect={onSelect}\n              />\n            ))}\n            {!rows.length && (\n              <tr>\n                <td colSpan={5}>No hay propuestas {title.toLowerCase()}.</td>\n              </tr>\n            )}\n          </tbody>\n        </table>\n      </div>\n    </section>\n  );\n}\nfunction accessibleChangeValue(value: string | number | null, money = false) {\n  if (value === null) return \"sin valor\";\n  return money\n    ? new Intl.NumberFormat(\"es-PE\", {\n        style: \"currency\",\n        currency: \"PEN\",\n      }).format(value as number)\n    : String(value);\n}\nfunction ProposalChange({ proposal }: { proposal: CatalogProposal }) {\n  const change = catalogProposalChange(proposal);\n  if (change.kind === \"price\") {\n    const label = `Anterior: ${accessibleChangeValue(change.previous, true)}; cambia a nuevo: ${accessibleChangeValue(change.next, true)}`;\n    return (\n      <>\n        <span aria-hidden=\"true\">\n          <Value value={change.previous} money /> <span>→</span>{\" \"}\n          <Value value={change.next} money />\n        </span>\n        <span className=\"admin-catalog__change-sr\">{label}</span>\n      </>\n    );\n  }\n  if (change.kind === \"text\") {\n    const label = `Anterior: ${accessibleChangeValue(change.previous)}; cambia a nuevo: ${accessibleChangeValue(change.next)}`;\n    return (\n      <>\n        <span aria-hidden=\"true\">\n          {change.previous ?? \"—\"} <span>→</span> {change.next ?? \"—\"}\n        </span>\n        <span className=\"admin-catalog__change-sr\">{label}</span>\n      </>\n    );\n  }\n  return <>{change.label}</>;\n}\nfunction ProposalRow({\n  proposal,\n  onSelect,\n}: {\n  proposal: CatalogProposal;\n  onSelect: (proposal: CatalogProposal) => void;\n}) {\n  const product = proposal.producto ?? proposal.catalogo_actual.producto ?? \"—\";\n  const origin =\n    proposal.cambio_id === null\n      ? \"Automático\"\n      : proposal.sedes.map((site) => site.nombre).join(\", \") || \"Propuesto\";\n  return (\n    <tr>\n      <td>{proposalLabels[proposal.tipo]}</td>\n      <th scope=\"row\">\n        <span className=\"admin-catalog__product-name\">{product}</span>\n        <span className=\"admin-catalog__product-code\">\n          {proposal.c_interno}\n        </span>\n      </th>\n      <td className=\"admin-catalog__change\">\n        <ProposalChange proposal={proposal} />\n      </td>\n      <td>\n        <span className=\"admin-attribute-badge\">{origin}</span>\n      </td>\n      <td>\n        <button\n          type=\"button\"\n          className=\"button button--secondary\"\n          onClick={() => onSelect(proposal)}\n        >\n          <Eye size={16} aria-hidden=\"true\" />\n        </button>\n      </td>\n    </tr>\n  );\n}\n\nfunction ProposalDetail({\n  proposal,\n  onClose,\n}: {\n  proposal: CatalogProposal;\n  onClose: () => void;\n}) {\n  const store = useCatalogStore();\n  const [error, setError] = useState(\"\");\n  const [setup, setSetup] = useState(false);\n  const [price, setPrice] = useState(false);\n  const intent = store.intent();\n  const run = (action: ProposalAction) => {\n    setError(\"\");\n    void store\n      .mutation(\n        \"proposal_action\",\n        { propuesta_fingerprint: proposal.propuesta_fingerprint, action },\n        { proposal },\n      )\n      .then(onClose)\n      .catch((reason: unknown) =>\n        setError(catalogMutationError(store, reason, \"No se pudo actualizar la propuesta.\")),\n      );\n  };\n  const retry = () => {\n    setError(\"\");\n    void store\n      .retryMutation()\n      .then(onClose)\n      .catch((reason: unknown) =>\n        setError(catalogMutationError(store, reason, \"No se pudo confirmar la propuesta.\")),\n      );\n  };\n  const blocked =\n    proposal.estado === \"aprobado\" && proposal.block_reason !== null;\n  const setupTarget: ProductSetupTarget = {\n    propuesta_fingerprint: proposal.propuesta_fingerprint,\n    c_interno: proposal.c_interno,\n    producto:\n      proposal.producto ??\n      proposal.catalogo_actual.producto ??\n      `SKU ${proposal.c_interno}`,\n    precio: proposal.catalogo_actual.precio ?? 0,\n    tipo:\n      proposal.tipo === \"reincorporar_producto\"\n        ? \"reincorporar_producto\"\n        : \"agregar_producto\",\n  };\n  const canSetup =\n    proposal.tipo === \"agregar_producto\" ||\n    proposal.tipo === \"reincorporar_producto\";\n  return (\n    <>\n      <AdminDialog\n        title={proposal.producto ?? `Propuesta ${proposal.c_interno}`}\n        description={proposalLabels[proposal.tipo]}\n        onClose={onClose}\n        closeDisabled={!!intent?.pending}\n        wide\n      >\n        <div className=\"admin-catalog__proposal-context\">\n          <span>C. interno {proposal.c_interno}</span>\n          <span>\n            {proposal.cambio_id === null\n              ? \"Candidato automático\"\n              : `Cambio ${proposal.cambio_id}`}\n          </span>\n          <span>\n            {proposal.sedes.length\n              ? proposal.sedes.map((site) => site.nombre).join(\", \")\n              : \"Sin sedes asociadas\"}\n          </span>\n          <span>{proposal.occurrence_count} apariciones</span>\n        </div>\n        <dl className=\"admin-catalog__proposal-summary\">\n          <div>\n            <dt>Estado</dt>\n            <dd>{proposal.estado}</dd>\n          </div>\n          <div>\n            <dt>Sección</dt>\n            <dd>{proposal.seccion}</dd>\n          </div>\n          <div>\n            <dt>Primera evidencia</dt>\n            <dd>{adminTimestamp(proposal.first_seen_at)}</dd>\n          </div>\n          <div>\n            <dt>Última evidencia</dt>\n            <dd>{adminTimestamp(proposal.last_seen_at)}</dd>\n          </div>\n          <div>\n            <dt>Producto actual</dt>\n            <dd>{proposal.catalogo_actual.producto ?? \"—\"}</dd>\n          </div>\n          <div>\n            <dt>Precio actual</dt>\n            <dd>\n              <Value value={proposal.catalogo_actual.precio} money />\n            </dd>\n          </div>\n          <div>\n            <dt>Código de barras</dt>\n            <dd>{proposal.catalogo_actual.c_barras ?? \"—\"}</dd>\n          </div>\n          <div>\n            <dt>Grupo</dt>\n            <dd>{proposal.catalogo_actual.grupo ?? \"—\"}</dd>\n          </div>\n        </dl>\n        {proposal.stale && (\n          <p role=\"status\">\n            Existe evidencia posterior para este SKU y tipo; revisa la propuesta\n            antes de publicar.\n          </p>\n        )}\n        {proposal.estado === \"aprobado\" && (\n          <p role={blocked ? \"alert\" : \"status\"}>\n            {blocked\n              ? `No publicable: ${proposal.block_reason}`\n              : proposal.publicable\n                ? \"Lista para publicación.\"\n                : \"Aprobada; el estado de publicación aún no está disponible.\"}\n          </p>\n        )}\n        {intent && <CatalogMutationNotice onRetry={retry} />}\n        <div className=\"admin-v2-actions\">\n          {proposal.estado === \"pendiente\" && (\n            <>\n              <button\n                type=\"button\"\n                className=\"button\"\n                disabled={!!intent}\n                onClick={() => run(\"approve\")}\n              >\n                <Check size={16} aria-hidden=\"true\" />\n                Aprobar\n              </button>\n              <button\n                type=\"button\"\n                className=\"button button--secondary\"\n                disabled={!!intent}\n                onClick={() => run(\"ignore\")}\n              >\n                <EyeOff size={16} aria-hidden=\"true\" />\n                Ignorar propuesta\n              </button>\n            </>\n          )}\n          {proposal.estado === \"aprobado\" && (\n            <>\n              <button\n                type=\"button\"\n                className=\"button button--secondary\"\n                disabled={!!intent}\n                onClick={() => run(\"withdraw\")}\n              >\n                <Undo2 size={16} aria-hidden=\"true\" />\n                Retirar aprobación\n              </button>\n              {canSetup && (\n                <button\n                  type=\"button\"\n                  className=\"button\"\n                  disabled={!!intent}\n                  onClick={() => setSetup(true)}\n                >\n                  <Settings size={16} aria-hidden=\"true\" />\n                  {proposal.setup\n                    ? \"Actualizar configuración\"\n                    : \"Configurar producto\"}\n                </button>\n              )}\n              {proposal.tipo === \"precio\" && (\n                <button\n                  type=\"button\"\n                  className=\"button\"\n                  disabled={!!intent}\n                  onClick={() => setPrice(true)}\n                >\n                  <DollarSign size={16} aria-hidden=\"true\" />\n                  {proposal.price_resolution\n                    ? \"Actualizar resolución de precio\"\n                    : \"Resolver precio\"}\n                </button>\n              )}\n            </>\n          )}\n        </div>\n        {error && <p role=\"alert\">{error}</p>}\n      </AdminDialog>\n      {setup && (\n        <ProductSetupDialog\n          target={setupTarget}\n          onClose={() => setSetup(false)}\n          onComplete={onClose}\n        />\n      )}\n      {price && (\n        <PriceResolutionDialog\n          fingerprint={proposal.propuesta_fingerprint}\n          onClose={() => setPrice(false)}\n          onComplete={onClose}\n        />\n      )}\n    </>\n  );\n}\nfunction PriceResolutionDialog({\n  fingerprint,\n  onClose,\n  onComplete,\n}: {\n  fingerprint: string;\n  onClose: () => void;\n  onComplete: () => void;\n}) {\n  const store = useCatalogStore();\n  const query = useCatalogQuery(\"price_options\", {\n    propuesta_fingerprint: fingerprint,\n  });\n  const [resolution, setResolution] = useState<PriceResolution | \"\">(\"\");\n  const [packageAction, setPackageAction] = useState<\n    \"keep\" | \"clear\" | \"not_applicable\" | \"set\" | \"\"\n  >(\"\");\n  const [preparedValuation, setPreparedValuation] = useState<{\n    unidades_por_paquete: number;\n    precio_paquete: number;\n  } | null>(null);\n  const [valuation, setValuation] = useState(false);\n  const [error, setError] = useState(\"\");\n  const intent = store.intent();\n  if (!query.data)\n    return (\n      <AdminDialog\n        title=\"Resolver precio\"\n        onClose={onClose}\n        closeDisabled={!!intent?.pending}\n        wide\n      >\n        <QueryState {...query} />\n      </AdminDialog>\n    );\n  const options = query.data;\n  const initial = {\n    unitsPerPackage: options.grupo.unidades_por_paquete,\n    packagePrice: options.grupo.precio_paquete,\n  };\n  const selectResolution = (value: PriceResolution | \"\") => {\n    setResolution(value);\n    setPackageAction(\"\");\n    setPreparedValuation(null);\n    setError(\"\");\n  };\n  const chooseValuation = (decision: ValuationDecision) => {\n    if (decision.enabled) {\n      setPreparedValuation({\n        unidades_por_paquete: decision.unitsPerPackage!,\n        precio_paquete: decision.packagePrice!,\n      });\n      setPackageAction(\"set\");\n    } else setPackageAction(\"clear\");\n    setValuation(false);\n  };\n  const submit = () => {\n    if (!resolution) {\n      setError(\"Selecciona una resolución de precio.\");\n      return;\n    }\n    if (options.change_state !== \"aprobado\") {\n      setError(\"La propuesta ya no está aprobada. Actualiza la bandeja.\");\n      return;\n    }\n    if (!packageAction) {\n      setError(\"Decide explícitamente la valorización por paquete.\");\n      return;\n    }\n    if (packageAction === \"set\" && !preparedValuation) {\n      setError(\"Configura un valorizado válido antes de preparar.\");\n      return;\n    }\n    setError(\"\");\n    const done = () => onComplete();\n    const failed = (reason: unknown) => setError(store.intent() ? \"\" : priceErrorMessage(reason));\n    if (resolution === \"separate_sku\") {\n      const payload =\n        packageAction === \"set\"\n          ? {\n              propuesta_fingerprint: fingerprint,\n              resolution: \"separate_sku\" as const,\n              package_action: \"set\" as const,\n              ...preparedValuation!,\n            }\n          : {\n              propuesta_fingerprint: fingerprint,\n              resolution: \"separate_sku\" as const,\n              package_action:\n                packageAction === \"clear\"\n                  ? (\"clear\" as const)\n                  : (\"not_applicable\" as const),\n            };\n      void store.mutation(\"prepare_price\", payload).then(done).catch(failed);\n      return;\n    }\n    const payload =\n      packageAction === \"set\"\n        ? {\n            propuesta_fingerprint: fingerprint,\n            resolution,\n            package_action: \"set\" as const,\n            ...preparedValuation!,\n          }\n        : {\n            propuesta_fingerprint: fingerprint,\n            resolution,\n            package_action:\n              packageAction === \"clear\"\n                ? (\"clear\" as const)\n                : (\"keep\" as const),\n          };\n    void store.mutation(\"prepare_price\", payload).then(done).catch(failed);\n  };\n  const retry = () => {\n    setError(\"\");\n    void store\n      .retryMutation()\n      .then(onComplete)\n      .catch((reason: unknown) => setError(store.intent() ? \"\" : priceErrorMessage(reason)));\n  };\n  const canKeep = resolution !== \"separate_sku\";\n  return (\n    <>\n      <AdminDialog\n        title=\"Resolver precio\"\n        description={`C. interno ${options.c_interno}`}\n        onClose={onClose}\n        closeDisabled={!!intent?.pending}\n        wide\n      >\n        <p>\n          Precio propuesto: <Value value={options.nuevo_precio} money />. La\n          resolución y el valorizado quedan en staging; se aplicarán solo al\n          publicar Catálogo.\n        </p>\n        <dl className=\"admin-catalog__proposal-summary\">\n          <div>\n            <dt>Grupo</dt>\n            <dd>{options.grupo.nombre}</dd>\n          </div>\n          <div>\n            <dt>Precio de grupo</dt>\n            <dd>\n              <Value value={options.grupo.precio} money />\n            </dd>\n          </div>\n          <div>\n            <dt>Paquete actual</dt>\n            <dd>\n              {initial.unitsPerPackage && initial.packagePrice\n                ? `x${initial.unitsPerPackage} · S/ ${initial.packagePrice.toFixed(2)}`\n                : \"Sin valorizado\"}\n            </dd>\n          </div>\n          <div>\n            <dt>Resolución previa</dt>\n            <dd>\n              {options.prepared_resolution\n                ? \"Existe staging preparado\"\n                : \"Sin resolución preparada\"}\n            </dd>\n          </div>\n        </dl>\n        <div className=\"admin-v2-table admin-catalog__table\">\n          <table>\n            <thead>\n              <tr>\n                <th scope=\"col\">SKU</th>\n                <th scope=\"col\">Producto</th>\n                <th scope=\"col\">Precio</th>\n              </tr>\n            </thead>\n            <tbody>\n              {options.members.map((member) => (\n                <tr key={member.c_interno}>\n                  <td>{member.c_interno}</td>\n                  <th scope=\"row\">{member.producto}</th>\n                  <td>\n                    <Value value={member.precio} money />\n                  </td>\n                </tr>\n              ))}\n            </tbody>\n          </table>\n        </div>\n        <label>\n          Resolución\n          <select\n            value={resolution}\n            onChange={(event) =>\n              selectResolution(event.target.value as PriceResolution | \"\")\n            }\n          >\n            <option value=\"\">Seleccionar</option>\n            {options.options.map((option) => (\n              <option key={option} value={option}>\n                {resolutionLabels[option]}\n              </option>\n            ))}\n          </select>\n        </label>\n        {resolution && (\n          <fieldset>\n            <legend>Valorizado</legend>\n            {canKeep && (\n              <button\n                type=\"button\"\n                className=\"button button--secondary\"\n                aria-pressed={packageAction === \"keep\"}\n                onClick={() => setPackageAction(\"keep\")}\n              >\n                <Package size={16} aria-hidden=\"true\" />\n                Conservar valorizado\n              </button>\n            )}\n            <button\n              type=\"button\"\n              className=\"button button--secondary\"\n              aria-pressed={\n                packageAction === \"set\" || packageAction === \"clear\"\n              }\n              onClick={() => setValuation(true)}\n            >\n              <RefreshCw size={16} aria-hidden=\"true\" />\n              Actualizar valorizado\n            </button>\n            {resolution === \"separate_sku\" && (\n              <button\n                type=\"button\"\n                className=\"button button--secondary\"\n                aria-pressed={packageAction === \"not_applicable\"}\n                onClick={() => setPackageAction(\"not_applicable\")}\n              >\n                <CircleOff size={16} aria-hidden=\"true\" />\n                Sin valorizado\n              </button>\n            )}\n            {packageAction === \"clear\" && (\n              <p>El valorizado se eliminará al publicar.</p>\n            )}\n            {packageAction === \"set\" && preparedValuation && (\n              <p>\n                Se preparará x{preparedValuation.unidades_por_paquete} · S/{\" \"}\n                {preparedValuation.precio_paquete.toFixed(2)} al publicar.\n              </p>\n            )}\n          </fieldset>\n        )}\n        {intent && <CatalogMutationNotice onRetry={retry} />}\n        <button\n          type=\"button\"\n          className=\"button\"\n          disabled={!!intent || options.change_state !== \"aprobado\"}\n          onClick={submit}\n        >\n          <Wrench size={16} aria-hidden=\"true\" />\n          Preparar resolución\n        </button>\n        {error && <p role=\"alert\">{error}</p>}\n      </AdminDialog>\n      {valuation && (\n        <ValuationDialog\n          unitPrice={options.nuevo_precio}\n          initial={initial}\n          description=\"Este cambio queda en staging y se aplicará al publicar Catálogo.\"\n          pending={!!intent?.pending}\n          error={error}\n          onClose={() => setValuation(false)}\n          onConfirm={chooseValuation}\n        />\n      )}\n    </>\n  );\n}\nfunction PublicationDialog({ onClose }: { onClose: () => void }) {\n  const store = useCatalogStore();\n  const query = useCatalogQuery(\"publication_preview\", {});\n  const admin = useAdminStore().bootstrap?.identity.rol === \"admin\";\n  const preview = query.data?.preview;\n  const receipt = store.publication;\n  const publish = () => {\n    void store.publish().catch(() => {});\n  };\n  return (\n    <AdminDialog\n      title=\"Publicar catálogo\"\n      onClose={onClose}\n      closeDisabled={!!receipt.pending}\n      wide\n    >\n      {!preview ? (\n        <QueryState {...query} />\n      ) : (\n        <>\n          <p>{preview.codigo}</p>\n          <p>\n            Versión {preview.version_actual ?? \"sin publicación\"} →{\" \"}\n            {preview.version_nueva ?? \"pendiente\"} · {preview.cambios_total}{\" \"}\n            cambios · {preview.sku_actuales} → {preview.sku_resultantes} SKU\n          </p>\n          <dl className=\"admin-v2-data\">\n            {Object.entries(preview.cambios).map(([type, count]) => (\n              <div key={type}>\n                <dt>{proposalLabels[type as CatalogProposal[\"tipo\"]]}</dt>\n                <dd>{count}</dd>\n              </div>\n            ))}\n          </dl>\n          {preview.errores.map((error) => (\n            <p role=\"alert\" key={error}>\n              {error}\n            </p>\n          ))}\n          {preview.conflictos.map((conflict, index) => (\n            <p role=\"alert\" key={index}>\n              Conflicto: {JSON.stringify(conflict)}\n            </p>\n          ))}\n        </>\n      )}\n      {receipt.operationId && !receipt.error && !receipt.result && (\n        <AdminNotice tone=\"info\">\n          Publicación pendiente de confirmar. El reintento conserva la misma operación.\n        </AdminNotice>\n      )}\n      {receipt.error && (\n        <AdminNotice tone=\"error\">{receipt.error}</AdminNotice>\n      )}\n      {receipt.result && (\n        <AdminNotice tone={receipt.result.completion_recorded ? \"success\" : \"info\"}>\n          {receipt.result.codigo} · versión {receipt.result.version}\n          {receipt.result.completion_recorded\n            ? \"\"\n            : \" · commit confirmado; falta registrar el cierre, vuelve a recuperar esta operación.\"}\n        </AdminNotice>\n      )}\n      <button\n        type=\"button\"\n        className=\"button\"\n        disabled={\n          !admin || !!receipt.pending || (!receipt.operationId && !preview?.ok)\n        }\n        onClick={publish}\n      >\n        {receipt.pending ? (\n          <RefreshCw size={16} aria-hidden=\"true\" />\n        ) : receipt.operationId ? (\n          <RotateCcw size={16} aria-hidden=\"true\" />\n        ) : (\n          <Upload size={16} aria-hidden=\"true\" />\n        )}\n        {receipt.pending\n          ? \"Publicando…\"\n          : receipt.operationId\n            ? \"Recuperar publicación\"\n            : \"Confirmar publicación\"}\n      </button>\n      {!admin && (\n        <p>Solo admin puede publicar; moderador puede revisar el preview.</p>\n      )}\n    </AdminDialog>\n  );\n}\n\nexport function AdminCatalogV3() {\n  const [publishing, setPublishing] = useState(false);\n  const store = useCatalogStore();\n  return (\n    <section className=\"admin-catalog\">\n      <header className=\"admin-catalog__header\">\n        <div>\n          <CatalogStatus />\n        </div>\n        <button\n          type=\"button\"\n          className=\"button\"\n          onClick={() => setPublishing(true)}\n        >\n          {store.publication.operationId ? (\n            <RotateCcw size={16} aria-hidden=\"true\" />\n          ) : (\n            <ClipboardCheck size={16} aria-hidden=\"true\" />\n          )}\n          {store.publication.operationId\n            ? \"Recuperar publicación\"\n            : \"Revisar publicación\"}\n        </button>\n      </header>\n      <ProposalsSurface />\n      {publishing && <PublicationDialog onClose={() => setPublishing(false)} />}\n    </section>\n  );\n}\n"

      at <anonymous> (/home/runner/work/SOLOG/SOLOG/tests/admin-catalog-v3.structure.test.ts:50:231)

::error file=tests/admin-catalog-v3.structure.test.ts,line=50,col=231,title=error: expect(received).toContain(expected)::n"%0An\"}\n        </button>\n      </header>\n      <ProposalsSurface />\n      {publishing && <PublicationDialog onClose={() => setPublishing(false)} />}\n    </section>\n  );\n}\n"%0A%0A      at <anonymous> (/home/runner/work/SOLOG/SOLOG/tests/admin-catalog-v3.structure.test.ts:50:231)
(fail) Catálogo V3: estructura principal > expone estados UI, reintentos y controles accesibles de staging y publicación [0.61ms]
(pass) Catálogo V3: estructura principal > Productos opera sobre Master Data completo y conserva las mutaciones Catálogo V3 [0.23ms]
(pass) la tabla de propuestas compone identidad y Cambio sin lecturas adicionales [0.24ms]
(pass) Cambio conserva la flecha visual y expone una relación accesible anterior a nuevo [0.16ms]

::endgroup::

::group::tests/admin-primitives-post-review.test.ts:
(pass) Delta post-revisión: primitives compartidas Admin > QuickFilterChip usa una primitive común y preserva Control como consumidor de referencia [0.57ms]
(pass) Delta post-revisión: primitives compartidas Admin > StateView usa la primitive común de Catálogo también en Incidencias [0.48ms]
(pass) Delta post-revisión: primitives compartidas Admin > el delta permanece frontend-only y no agrega lecturas para los contadores [0.25ms]

::endgroup::

::group::tests/admin-catalog-v3.integration.test.ts:
(pass) Catálogo V3 integración > carga conjuntos completos por estado sin conservar la lectura Productos en el store activo [0.46ms]
(pass) Catálogo V3 integración > carga pendientes inicialmente y completa el alta con grupo existente hasta publicación [0.39ms]
(pass) Catálogo V3 integración > integra reincorporación, exclusión y grupo unitario en el flujo de propuestas [0.28ms]
(pass) Catálogo V3 integración > integra las tres resoluciones de precio y la decisión xN contractual [0.25ms]
(pass) Catálogo V3 integración > moderador consulta, revisa y prepara, pero no publica [0.19ms]
(pass) Catálogo V3 integración > expone loading/error/retry de lectura sin llamadas reales a Supabase [0.17ms]

::endgroup::

::group::tests/admin-feedback.test.ts:
(pass) Admin feedback de mutaciones > cada retry conserva operation_id pero incrementa la ocurrencia visual [0.45ms]
(pass) Admin feedback de mutaciones > MutationNotice usa AdminNotice y no presenta operation_id ni replay [0.21ms]
(pass) Admin feedback de mutaciones > dismiss se asocia a ocurrencia y no al texto visible [0.14ms]
(pass) Admin feedback fases 2 y 3 > Catálogo y Productos comparten presenter sin UUID ni replay visible [0.30ms]
(pass) Admin feedback fases 2 y 3 > errores retryable de Catálogo permanecen en una única superficie [0.19ms]
(pass) Admin feedback fases 2 y 3 > Dispositivos separa feedback modal y feedback de página [0.11ms]
130 |     expect(incidents).toContain('local:${feedbackOccurrence}')
131 |     expect(incidents).toContain('pendingIntent.attempt')
132 |     expect(incidents).toContain('retryIncidentIntent')
133 |     expect(incidents).toContain('store.retryMutation("incidents")')
134 |     expect(incidents).toContain('onDismiss={pendingIntent ? undefined')
135 |     expect(incidents).toContain('retryable ? store.retryMutation("incidents")')
                            ^
error: expect(received).toContain(expected)

Expected to contain: "retryable ? store.retryMutation(\"incidents\")"
Received: "import { useCallback, useEffect, useMemo, useRef, useState } from \"react\";\nimport {\n  AlarmClockOff,\n  ChevronLeft,\n  ChevronRight,\n  CircleOff,\n  Eye,\n  RotateCcw,\n} from \"lucide-react\";\nimport { useAdminStore } from \"../admin.v2.context\";\nimport { useManagement, useManagementQuery } from \"../admin.management.context\";\nimport { AdminDialog } from \"../admin.dialog\";\nimport { ReadNotice } from \"../admin.management.presentation\";\nimport { AdminNotice } from \"../admin.primitives\";\nimport {\n  incidentSiteAbbreviation,\n  incidentTimestamp,\n} from \"./admin.incidents.presentation\";\nimport { orderedAdminSites } from \"../admin.site-ui\";\nimport {\n  incidentAllScopeActive,\n  loadIncidentSummaries,\n  mergeIncidentSummaries,\n  nextIncidentSummaryExpiry,\n  reconcileIncidentAllScope,\n  toggleIncidentAllScope,\n  type IncidentFamilySource,\n  type MergedIncidentFamily,\n} from \"./admin.incidents.multisite\";\nimport type {\n  Family,\n  IncidentState,\n  IncidentType,\n} from \"../admin.management.v2\";\n\nconst typeLabels: Record<IncidentType, string> = {\n  producto_ausente: \"Producto ausente\",\n  codigo_interno_invalido: \"C.interno inválido\",\n  codigo_interno_duplicado: \"C.interno duplicado\",\n  stock_invalido: \"Stock inválido\",\n};\nconst stateLabels: Record<IncidentState, string> = {\n  pendiente: \"Pendiente\",\n  suprimida: \"Suprimida\",\n  resuelta: \"Resuelta\",\n};\nconst stateViews: Array<{ state: IncidentState; label: string }> = [\n  { state: \"pendiente\", label: \"Pendientes\" },\n  { state: \"suprimida\", label: \"Suprimidas\" },\n  { state: \"resuelta\", label: \"Resueltas\" },\n];\n\nfunction StateBadge({ state }: { state: IncidentState }) {\n  return (\n    <span\n      className={`admin-incidents__state admin-status-badge admin-status-badge--${state === \"pendiente\" ? \"warning\" : state === \"resuelta\" ? \"success\" : \"info\"} admin-incidents__state--${state}`}\n    >\n      {stateLabels[state]}\n    </span>\n  );\n}\n\nfunction FamilyDetail({\n  family,\n  site,\n  onClose,\n}: {\n  family: Family;\n  site?: string;\n  onClose: () => void;\n}) {\n  const [page, setPage] = useState(0);\n  const query = useManagementQuery(\"detail\", {\n    family_key: family.family_key,\n    ...(site ? { site_id: site } : {}),\n    page,\n    page_size: 100,\n  });\n  return (\n    <AdminDialog\n      title={`Repeticiones · ${typeLabels[family.tipo]}`}\n      description={\n        site\n          ? \"Detalle solicitado bajo demanda para una sede fuente.\"\n          : \"Detalle solicitado bajo demanda para todas las sedes.\"\n      }\n      onClose={onClose}\n      wide\n    >\n      {query.data ? (\n        <>\n          <div\n            className=\"admin-v2-table admin-incidents__detail-table\"\n            role=\"region\"\n            aria-label=\"Detalle de repeticiones\"\n            tabIndex={0}\n          >\n            <table>\n              <thead>\n                <tr>\n                  <th>Sede</th>\n                  <th>Estado</th>\n                  <th>Vigencia</th>\n                  <th>Primera detección</th>\n                  <th>Última detección</th>\n                  <th>Resuelta</th>\n                  <th>Apariciones</th>\n                  <th>Datos</th>\n                </tr>\n              </thead>\n              <tbody>\n                {query.data.items.map((item) => (\n                  <tr key={item.id}>\n                    <td>{item.sede}</td>\n                    <td>\n                      <StateBadge state={item.estado} />\n                    </td>\n                    <td>{item.active ? \"Vigente\" : \"Histórica\"}</td>\n                    <td>{incidentTimestamp(item.first_seen_at)}</td>\n                    <td>{incidentTimestamp(item.last_seen_at)}</td>\n                    <td>\n                      {item.resuelta_at\n                        ? incidentTimestamp(item.resuelta_at)\n                        : \"—\"}\n                    </td>\n                    <td>{item.occurrence_count}</td>\n                    <td>\n                      <details>\n                        <summary>Ver datos</summary>\n                        <pre className=\"admin-v2-json\">\n                          {JSON.stringify(item.datos, null, 2)}\n                        </pre>\n                      </details>\n                    </td>\n                  </tr>\n                ))}\n              </tbody>\n            </table>\n          </div>\n          <div className=\"admin-v2-toolbar\">\n            <button\n              type=\"button\"\n              className=\"button button--secondary\"\n              disabled={!page}\n              onClick={() => setPage(page - 1)}\n            >\n              <ChevronLeft size={16} aria-hidden=\"true\" />\n              Anterior\n            </button>\n            <span>Página {page + 1}</span>\n            <button\n              type=\"button\"\n              className=\"button button--secondary\"\n              disabled={query.data.items.length < 100}\n              onClick={() => setPage(page + 1)}\n            >\n              Siguiente\n              <ChevronRight size={16} aria-hidden=\"true\" />\n            </button>\n          </div>\n        </>\n      ) : (\n        <ReadNotice {...query} />\n      )}\n    </AdminDialog>\n  );\n}\n\nfunction canProposeDelete(family: Family) {\n  return (\n    family.tipo === \"producto_ausente\" &&\n    family.active &&\n    family.family_state === \"pendiente\" &&\n    family.c_interno !== null &&\n    Number.isSafeInteger(family.c_interno) &&\n    family.c_interno > 0 &&\n    !family.deletion_proposed\n  );\n}\n\nfunction globalIncidentRevision(\n  store: ReturnType<typeof useManagement>,\n  family: MergedIncidentFamily,\n) {\n  const revisions = [\n    ...new Set(\n      family.sources\n        .map(\n          (source) =>\n            store.peek(\"summary\", { site_id: source.siteId }).data?.revisions\n              .incidents_global,\n        )\n        .filter((value): value is number => typeof value === \"number\"),\n    ),\n  ];\n  return revisions.length === 1 ? revisions[0] : null;\n}\n\nfunction IgnoreDialog({\n  family,\n  onClose,\n  onSuccess,\n}: {\n  family: MergedIncidentFamily;\n  onClose: () => void;\n  onSuccess: () => void;\n}) {\n  const store = useManagement();\n  const [running, setRunning] = useState(false);\n  const [error, setError] = useState(\"\");\n  const retryable = store.intent(\"incidents\")?.action === \"ignore_30d\";\n  const ignore = () => {\n    const revision = globalIncidentRevision(store, family);\n    if (!retryable && revision === null) {\n      setError(\n        \"La revisión global de incidencias no está disponible. Actualiza la vista y reintenta.\",\n      );\n      return;\n    }\n    setError(\"\");\n    setRunning(true);\n    const request = retryable\n      ? store.retryMutation(\"incidents\")\n      : store.mutation(\n          \"ignore_30d\",\n          {\n            family_key: family.family_key,\n            scope: \"global\",\n          },\n          revision!,\n        );\n    void request\n      .then(() => {\n        onSuccess();\n        onClose();\n      })\n      .catch((reason) =>\n        setError(\n          reason instanceof Error\n            ? reason.message\n            : \"No se pudo ignorar la incidencia.\",\n        ),\n      )\n      .finally(() => setRunning(false));\n  };\n  return (\n    <AdminDialog\n      title=\"Ignorar incidencia durante 30 días\"\n      description={`${typeLabels[family.tipo]} · ${family.c_interno ?? family.c_interno_original ?? \"Sin código\"}`}\n      onClose={onClose}\n      closeDisabled={running}\n      footer={\n        <>\n          <button\n            type=\"button\"\n            className=\"button button--secondary\"\n            disabled={running}\n            onClick={onClose}\n          >\n            Cancelar\n          </button>\n          <button\n            type=\"button\"\n            className=\"button button--primary\"\n            disabled={running}\n            onClick={ignore}\n          >\n            <AlarmClockOff size={16} aria-hidden=\"true\" />\n            {running ? \"Procesando…\" : retryable ? \"Reintentar\" : \"Ignorar 30 días\"}\n          </button>\n        </>\n      }\n    >\n      <p>Esta incidencia se ignorará durante 30 días en todas las sedes.</p>\n      {error && <AdminNotice tone=\"error\">{error}</AdminNotice>}\n    </AdminDialog>\n  );\n}\n\ntype DeleteProposal = {\n  family: MergedIncidentFamily;\n  source: IncidentFamilySource;\n};\n\nfunction DeleteProposalDialog({\n  proposal,\n  onClose,\n  onConfirm,\n  onRetry,\n  retryable,\n}: {\n  proposal: DeleteProposal;\n  onClose: () => void;\n  onConfirm: () => Promise<void>;\n  onRetry: () => Promise<void>;\n  retryable: boolean;\n}) {\n  const [proposing, setProposing] = useState(false);\n  const [error, setError] = useState(\"\");\n  const product =\n    typeof proposal.source.family.datos.producto === \"string\"\n      ? proposal.source.family.datos.producto\n      : null;\n  const confirm = () => {\n    if (proposing) return;\n    setError(\"\");\n    setProposing(true);\n    void (retryable ? onRetry() : onConfirm())\n      .then(onClose)\n      .catch((reason) =>\n        setError(\n          reason instanceof Error\n            ? reason.message\n            : \"No se pudo proponer la eliminación.\",\n        ),\n      )\n      .finally(() => setProposing(false));\n  };\n  return (\n    <AdminDialog\n      title=\"Proponer eliminación\"\n      description=\"Se creará una propuesta de eliminación para revisión en Catálogo.\"\n      onClose={onClose}\n      closeDisabled={proposing}\n      footer={\n        <>\n          <button\n            type=\"button\"\n            className=\"button button--secondary\"\n            disabled={proposing}\n            onClick={onClose}\n          >\n            Cancelar\n          </button>\n          <button\n            type=\"button\"\n            className=\"button button--danger\"\n            disabled={proposing}\n            onClick={confirm}\n          >\n            {proposing ? \"Procesando…\" : retryable ? \"Reintentar\" : \"Proponer eliminación\"}\n          </button>\n        </>\n      }\n    >\n      <p>\n        <strong>\n          Producto ausente ·{\" \"}\n          {proposal.source.family.c_interno ??\n            proposal.source.family.c_interno_original ??\n            \"Sin código\"}\n        </strong>\n      </p>\n      {product && <p>{product}</p>}\n      <p>\n        Detectado en: <strong>{proposal.source.siteName}</strong>\n      </p>\n      {error && <AdminNotice tone=\"error\">{error}</AdminNotice>}\n    </AdminDialog>\n  );\n}\nexport function AdminIncidentsV2() {\n  const admin = useAdminStore();\n  const store = useManagement();\n  const [type, setType] = useState<\"all\" | IncidentType>(\"all\");\n  const [state, setState] = useState<IncidentState>(\"pendiente\");\n  const [detailFamily, setDetailFamily] = useState<MergedIncidentFamily | null>(\n    null,\n  );\n  const [ignoreFamily, setIgnoreFamily] = useState<MergedIncidentFamily | null>(\n    null,\n  );\n  const [deleteProposal, setDeleteProposal] = useState<DeleteProposal | null>(\n    null,\n  );\n  const [allOriginSite, setAllOriginSite] = useState<string | null>(null);\n  const [allLoading, setAllLoading] = useState(false);\n  const [error, setErrorState] = useState(\"\");\n  const [notice, setNoticeState] = useState(\"\");\n  const [feedbackOccurrence, setFeedbackOccurrence] = useState(0);\n  const [dismissedNotice, setDismissedNotice] = useState(\"\");\n  const setError = (message: string) => {\n    setErrorState(message);\n    if (message) setFeedbackOccurrence((current) => current + 1);\n  };\n  const setNotice = (message: string) => {\n    setNoticeState(message);\n    if (message) setFeedbackOccurrence((current) => current + 1);\n  };\n  const allLoadRef = useRef<Promise<void> | null>(null);\n  const [allSnapshot, setAllSnapshot] = useState<MergedIncidentFamily[]>([]);\n  const siteId = admin.siteId;\n  const allActive = incidentAllScopeActive(allOriginSite, siteId);\n  const normalQuery = useManagementQuery(\n    \"summary\",\n    { site_id: siteId },\n    { enabled: !!siteId && !allActive },\n  );\n  const sites = useMemo(\n    () => orderedAdminSites(admin.bootstrap?.allowed_sites ?? []),\n    [admin.bootstrap],\n  );\n  const ensureAllSummaries = useCallback(() => {\n    if (allLoadRef.current) return allLoadRef.current;\n    setAllLoading(true);\n    const request = loadIncidentSummaries(store, sites).then(() => undefined);\n    allLoadRef.current = request;\n    const cleanup = () => {\n      if (allLoadRef.current === request) allLoadRef.current = null;\n      setAllLoading(false);\n    };\n    void request.then(cleanup, cleanup);\n    return request;\n  }, [sites, store]);\n\n  useEffect(() => {\n    const reconciled = reconcileIncidentAllScope(allOriginSite, siteId);\n    if (reconciled === allOriginSite) return;\n    const cancel = window.setTimeout(() => setAllOriginSite(reconciled), 0);\n    return () => window.clearTimeout(cancel);\n  }, [allOriginSite, siteId]);\n\n  const allSources = sites\n    .map((site) => {\n      const summary = store.peek(\"summary\", { site_id: site.id }).data;\n      return summary\n        ? { siteId: site.id, siteName: site.nombre, summary }\n        : null;\n    })\n    .filter((value): value is NonNullable<typeof value> => value !== null);\n  const allComplete = sites.length > 0 && allSources.length === sites.length;\n  const mergedAll = useMemo(\n    () => (allComplete ? mergeIncidentSummaries(allSources) : null),\n    [allComplete, allSources],\n  );\n  useEffect(() => {\n    if (!allActive || !mergedAll) return;\n    const update = window.setTimeout(() => setAllSnapshot(mergedAll), 0);\n    return () => window.clearTimeout(update);\n  }, [allActive, mergedAll]);\n\n  const allNextExpiry = nextIncidentSummaryExpiry(store, sites);\n  useEffect(() => {\n    if (!allActive || !sites.length) return;\n\n    const refreshIfNeeded = () => {\n      const missing = sites.some(\n        (site) => !store.peek(\"summary\", { site_id: site.id }).data,\n      );\n      if (!missing) return;\n      setError(\"\");\n      setNotice(\"\");\n      void ensureAllSummaries().catch((reason) => {\n        setAllOriginSite(null);\n        setError(\n          reason instanceof Error\n            ? reason.message\n            : \"No se pudo actualizar todas las sedes.\",\n        );\n      });\n    };\n\n    if (!allComplete) {\n      refreshIfNeeded();\n      return;\n    }\n\n    const delay =\n      allNextExpiry === undefined\n        ? null\n        : Math.max(0, allNextExpiry - Date.now()) + 25;\n    const timer =\n      delay === null ? null : window.setTimeout(refreshIfNeeded, delay);\n    const onPageShow = () => refreshIfNeeded();\n    const onVisibility = () => {\n      if (document.visibilityState === \"visible\") refreshIfNeeded();\n    };\n    window.addEventListener(\"pageshow\", onPageShow);\n    document.addEventListener(\"visibilitychange\", onVisibility);\n    return () => {\n      if (timer !== null) window.clearTimeout(timer);\n      window.removeEventListener(\"pageshow\", onPageShow);\n      document.removeEventListener(\"visibilitychange\", onVisibility);\n    };\n  }, [allActive, allComplete, allNextExpiry, ensureAllSummaries, sites, store]);\n\n  const displayedFamilies = useMemo(() => {\n    if (allActive) return mergedAll ?? allSnapshot;\n    if (!normalQuery.data || !siteId) return [];\n    const siteName = sites.find((site) => site.id === siteId)?.nombre ?? siteId;\n    return mergeIncidentSummaries([\n      { siteId, siteName, summary: normalQuery.data },\n    ]);\n  }, [allActive, allSnapshot, mergedAll, normalQuery.data, siteId, sites]);\n  const families = useMemo(\n    () =>\n      displayedFamilies.filter(\n        (item) =>\n          (type === \"all\" || item.tipo === type) && item.family_state === state,\n      ),\n    [displayedFamilies, state, type],\n  );\n  const stateCounts = useMemo(() => {\n    const available = displayedFamilies.filter(\n      (item) => type === \"all\" || item.tipo === type,\n    );\n    return {\n      pendiente: available.filter((item) => item.family_state === \"pendiente\")\n        .length,\n      suprimida: available.filter((item) => item.family_state === \"suprimida\")\n        .length,\n      resuelta: available.filter((item) => item.family_state === \"resuelta\")\n        .length,\n    };\n  }, [displayedFamilies, type]);\n  const toggleAll = async () => {\n    const nextOrigin = toggleIncidentAllScope(allOriginSite, siteId);\n    if (nextOrigin === null) {\n      setAllOriginSite(null);\n      return;\n    }\n    setError(\"\");\n    setNotice(\"\");\n    try {\n      await ensureAllSummaries();\n      setAllOriginSite(nextOrigin);\n    } catch (reason) {\n      setError(\n        reason instanceof Error\n          ? reason.message\n          : \"No se pudo cargar todas las sedes.\",\n      );\n    }\n  };\n  const reactivateFamily = (family: MergedIncidentFamily): Promise<void> => {\n    const revision = globalIncidentRevision(store, family);\n    if (revision === null) {\n      const reason = new Error(\n        \"La revisión global de incidencias no está disponible. Actualiza la vista y reintenta.\",\n      );\n      setError(reason.message);\n      return Promise.reject(reason);\n    }\n    setError(\"\");\n    setNotice(\"\");\n    return store\n      .mutation(\n        \"reactivate\",\n        {\n          family_key: family.family_key,\n          scope: \"global\",\n        },\n        revision,\n      )\n      .then(() => {\n        setAllSnapshot([]);\n        setNotice(\"La incidencia fue reactivada.\");\n      })\n      .catch((reason) => {\n        if (!store.intent(\"incidents\")) {\n          setError(\n            reason instanceof Error\n              ? reason.message\n              : \"No se pudo actualizar la incidencia.\",\n          );\n        }\n        throw reason;\n      });\n  };\n  const proposeDeleteSource = (\n    source: IncidentFamilySource,\n  ): Promise<void> => {\n    const summary = store.peek(\"summary\", { site_id: source.siteId }).data;\n    if (!summary) {\n      return Promise.reject(\n        new Error(\"La sede ya no tiene un summary vigente. Reintenta la acción.\"),\n      );\n    }\n    setError(\"\");\n    setNotice(\"\");\n    return store\n      .mutation(\n        \"propose_delete\",\n        {\n          family_key: source.family.family_key,\n          scope: \"site\",\n          site_id: source.siteId,\n        },\n        summary.revisions.incidents,\n        source.siteId,\n      )\n      .then(() => {\n        setNotice(\n          \"La propuesta de eliminación quedó pendiente para revisión en Catálogo.\",\n        );\n      })\n      .catch((reason) => {\n        throw reason;\n      });\n  };\n  const pendingIntent = store.intent(\"incidents\");\n  const pending = !!pendingIntent;\n  const retryIncidentIntent = (): Promise<void> => {\n    const intent = store.intent(\"incidents\");\n    if (!intent) return Promise.reject(new Error(\"No hay una operación pendiente.\"));\n    setError(\"\");\n    setNotice(\"\");\n    return store.retryMutation(\"incidents\").then(() => {\n      setAllSnapshot([]);\n      const success =\n        intent.action === \"ignore_30d\"\n          ? \"La incidencia fue ignorada durante 30 días.\"\n          : intent.action === \"reactivate\"\n            ? \"La incidencia fue reactivada.\"\n            : \"La propuesta de eliminación quedó pendiente para revisión en Catálogo.\";\n      setNotice(success);\n    }).catch((reason) => {\n      if (!store.intent(\"incidents\")) {\n        setError(\n          reason instanceof Error\n            ? reason.message\n            : \"No se pudo actualizar la incidencia.\",\n        );\n      }\n      throw reason;\n    });\n  };\n  const noticeMessage = error || pendingIntent?.error || notice;\n  const noticeKey = pendingIntent\n    ? `intent:${String(pendingIntent.payload.operation_id)}:${pendingIntent.attempt}:${pendingIntent.error ? \"error\" : \"pending\"}`\n    : `local:${feedbackOccurrence}`;\n  const hasData = allActive || !!normalQuery.data;\n  return (\n    <section className=\"admin-incidents\">\n      <div className=\"admin-incidents__controls\">\n        <div\n          className=\"admin-state-views\"\n          role=\"tablist\"\n          aria-label=\"Estado de incidencias\"\n          onKeyDown={(event) => {\n            if (\n              ![\n                \"ArrowRight\",\n                \"ArrowDown\",\n                \"ArrowLeft\",\n                \"ArrowUp\",\n                \"Home\",\n                \"End\",\n              ].includes(event.key)\n            )\n              return;\n            event.preventDefault();\n            const currentIndex = stateViews.findIndex(\n              (view) => view.state === state,\n            );\n            const direction =\n              event.key === \"ArrowRight\" || event.key === \"ArrowDown\" ? 1 : -1;\n            const nextIndex =\n              event.key === \"Home\"\n                ? 0\n                : event.key === \"End\"\n                  ? stateViews.length - 1\n                  : (currentIndex + direction + stateViews.length) %\n                    stateViews.length;\n            const nextView = stateViews[nextIndex];\n            setState(nextView.state);\n            event.currentTarget\n              .querySelector<HTMLButtonElement>(\n                `#admin-incidents-state-${nextView.state}`,\n              )\n              ?.focus();\n          }}\n        >\n          {stateViews.map((view) => (\n            <button\n              key={view.state}\n              id={`admin-incidents-state-${view.state}`}\n              type=\"button\"\n              role=\"tab\"\n              aria-selected={state === view.state}\n              aria-controls=\"admin-incidents-state-panel\"\n              tabIndex={state === view.state ? 0 : -1}\n              onClick={() => setState(view.state)}\n            >\n              <span>{view.label}</span>\n              <strong>{stateCounts[view.state]}</strong>\n            </button>\n          ))}\n        </div>\n        <label className=\"admin-toolbar__filter admin-incidents__type\">\n          Tipo:\n          <select\n            value={type}\n            onChange={(event) =>\n              setType(event.target.value as \"all\" | IncidentType)\n            }\n          >\n            <option value=\"all\">Todos</option>\n            {Object.entries(typeLabels).map(([value, label]) => (\n              <option key={value} value={value}>\n                {label}\n              </option>\n            ))}\n          </select>\n        </label>\n        <button\n          type=\"button\"\n          className=\"button button--primary\"\n          aria-pressed={allActive}\n          disabled={allLoading || !sites.length}\n          onClick={() => void toggleAll()}\n        >\n          {allLoading ? \"Cargando sedes…\" : \"Todas las sedes\"}\n        </button>\n      </div>\n      {noticeMessage && dismissedNotice !== noticeKey && (\n        <AdminNotice\n          tone={error || pendingIntent?.error ? \"error\" : \"success\"}\n          onDismiss={pendingIntent ? undefined : () => setDismissedNotice(noticeKey)}\n          action={\n            pendingIntent && !pendingIntent.pending ? (\n              <button\n                type=\"button\"\n                className=\"button button--secondary\"\n                onClick={() => void retryIncidentIntent().catch(() => {})}\n              >\n                Reintentar\n              </button>\n            ) : undefined\n          }\n        >\n          {error || pendingIntent?.error || notice}\n        </AdminNotice>\n      )}{\" \"}\n      {hasData ? (\n        <div className=\"admin-table-section admin__table\">\n          <div\n            className=\"admin-v2-table\"\n            role=\"tabpanel\"\n            id=\"admin-incidents-state-panel\"\n            aria-labelledby={`admin-incidents-state-${state}`}\n            tabIndex={0}\n          >\n            <table>\n              <thead>\n                <tr>\n                  <th>Tipo</th>\n                  <th>Producto</th>\n                  <th>Sedes</th>\n                  <th>\n                    {state === \"pendiente\"\n                      ? \"Última detección\"\n                      : state === \"suprimida\"\n                        ? \"Ignorada hasta\"\n                        : \"Resuelta\"}\n                  </th>\n                  <th>Acciones</th>\n                </tr>\n              </thead>\n              <tbody>\n                {families.map((item) => {\n                  const product =\n                    typeof item.datos.producto === \"string\" &&\n                    item.datos.producto.trim()\n                      ? item.datos.producto\n                      : \"Producto sin nombre\";\n                  const date =\n                    state === \"pendiente\"\n                      ? item.last_seen_at\n                      : state === \"suprimida\"\n                        ? item.active_suppression_until\n                        : item.resolved_at;\n                  const reactivable = item.active_suppression_until !== null;\n                  const deletable = item.sources.find((source) =>\n                    canProposeDelete(source.family),\n                  );\n                  return (\n                    <tr key={item.family_key}>\n                      <td>\n                        <span className=\"admin-attribute-badge admin-incidents__type-badge\">\n                          {typeLabels[item.tipo]}\n                        </span>\n                      </td>\n                      <th scope=\"row\" className=\"admin-incidents__product\">\n                        <span>{product}</span>\n                        <small>\n                          {item.c_interno ??\n                            item.c_interno_original ??\n                            \"Sin código\"}\n                        </small>\n                      </th>\n                      <td>\n                        <div className=\"admin-incidents__sites\">\n                          {item.sources.map((source) => (\n                            <span\n                              key={source.siteId}\n                              className=\"admin-attribute-badge\"\n                            >\n                              {incidentSiteAbbreviation(source.siteName)}\n                            </span>\n                          ))}\n                        </div>\n                      </td>\n                      <td>{incidentTimestamp(date)}</td>\n                      <td>\n                        <div className=\"admin-v2-actions\">\n                          <button\n                            type=\"button\"\n                            className=\"icon-button\"\n                            aria-label={`Ver repeticiones ${item.family_key}`}\n                            title=\"Ver detalle\"\n                            onClick={() => setDetailFamily(item)}\n                          >\n                            <Eye size={16} aria-hidden=\"true\" />\n                          </button>\n                          {state === \"pendiente\" &&\n                            item.active &&\n                            !item.sources.every(\n                              (source) => source.family.reactivate_available,\n                            ) && (\n                              <button\n                                type=\"button\"\n                                className=\"icon-button icon-button--warning\"\n                                aria-label=\"Ignorar 30 días\"\n                                title=\"Ignorar 30 días\"\n                                disabled={pending}\n                                onClick={() => setIgnoreFamily(item)}\n                              >\n                                <AlarmClockOff size={16} aria-hidden=\"true\" />\n                              </button>\n                            )}\n                          {state === \"pendiente\" && deletable && (\n                            <button\n                              type=\"button\"\n                              className=\"icon-button icon-button--danger\"\n                              aria-label=\"Proponer eliminación\"\n                              title=\"Proponer eliminación\"\n                              disabled={pending}\n                              onClick={() =>\n                                setDeleteProposal({\n                                  family: item,\n                                  source: deletable,\n                                })\n                              }\n                            >\n                              <CircleOff size={16} aria-hidden=\"true\" />\n                            </button>\n                          )}\n                          {state === \"suprimida\" && reactivable && (\n                            <button\n                              type=\"button\"\n                              className=\"icon-button\"\n                              aria-label=\"Reactivar incidencia\"\n                              title=\"Reactivar incidencia\"\n                              disabled={pending}\n                              onClick={() =>\n                                void reactivateFamily(item).catch(() => {})\n                              }\n                            >\n                              <RotateCcw size={16} aria-hidden=\"true\" />\n                            </button>\n                          )}\n                        </div>\n                      </td>\n                    </tr>\n                  );\n                })}{\" \"}\n                {!families.length && (\n                  <tr>\n                    <td colSpan={5}>\n                      No hay incidencias que coincidan con los filtros locales.\n                    </td>\n                  </tr>\n                )}\n              </tbody>\n            </table>\n          </div>\n        </div>\n      ) : (\n        <ReadNotice {...normalQuery} />\n      )}\n      {detailFamily && (\n        <FamilyDetail\n          key={`${allActive ? \"all\" : siteId}:${detailFamily.family_key}`}\n          family={detailFamily}\n          site={allActive ? undefined : siteId}\n          onClose={() => setDetailFamily(null)}\n        />\n      )}\n      {ignoreFamily && (\n        <IgnoreDialog\n          family={ignoreFamily}\n          onClose={() => setIgnoreFamily(null)}\n          onSuccess={() => {\n            setAllSnapshot([]);\n            setNotice(\"La incidencia fue ignorada durante 30 días.\");\n          }}\n        />\n      )}\n      {deleteProposal && (\n        <DeleteProposalDialog\n          proposal={deleteProposal}\n          onClose={() => setDeleteProposal(null)}\n          onConfirm={() => proposeDeleteSource(deleteProposal.source)}\n          onRetry={retryIncidentIntent}\n          retryable={pendingIntent?.action === \"propose_delete\"}\n        />\n      )}\n    </section>\n  );\n}\n"

      at <anonymous> (/home/runner/work/SOLOG/SOLOG/tests/admin-feedback.test.ts:135:23)

::error file=tests/admin-feedback.test.ts,line=135,col=23,title=error: expect(received).toContain(expected)::Expected to contain: "retryable ? store.retryMutation(\"incidents\")"%0Aas.\");\n          }}\n        />\n      )}\n      {deleteProposal && (\n        <DeleteProposalDialog\n          proposal={deleteProposal}\n          onClose={() => setDeleteProposal(null)}\n          onConfirm={() => proposeDeleteSource(deleteProposal.source)}\n          onRetry={retryIncidentIntent}\n          retryable={pendingIntent?.action === \"propose_delete\"}\n        />\n      )}\n    </section>\n  );\n}\n"%0A%0A      at <anonymous> (/home/runner/work/SOLOG/SOLOG/tests/admin-feedback.test.ts:135:23)
(fail) Admin feedback fases 2 y 3 > Incidencias usa ocurrencia independiente del texto y retry de la intención original [0.59ms]
(pass) Admin feedback fases 2 y 3 > notices retryable no pueden ocultar la única acción de retry [0.16ms]

::endgroup::

::group::tests/admin-management-isolation.test.ts:
(pass) normalized transport errors preserve the original operation, not just raw network errors [0.43ms]
(pass) master revision conflict clears the rejected intent and refreshes its source [0.23ms]
(pass) incidents revision conflict clears the rejected intent and refreshes its source [0.07ms]
(pass) authorization rejection clears data and notifies the Admin shell [0.22ms]
(pass) late response cannot cross a role change [0.16ms]
(pass) incident period expiration is anchored to backend time and leaves devices cached [0.27ms]
(pass) mutation invalidates an older in-flight family detail without resurrecting it [0.25ms]
(pass) Edge failure releases receipt only with a rejected preview or failed-ledger replay [0.15ms]
(pass) different Auth users never share a management dataset [0.15ms]
(pass) rejections de evidencia o supresión obsoleta descartan la intención y fuerzan una recarga autoritativa [0.35ms]

::endgroup::

3 tests failed:
(fail) Incidencias multisede > CircleOff difiere propose_delete hasta la confirmación con la fuente seleccionada [1.02ms]
(fail) Catálogo V3: estructura principal > expone estados UI, reintentos y controles accesibles de staging y publicación [0.61ms]
(fail) Admin feedback fases 2 y 3 > Incidencias usa ocurrencia independiente del texto y retry de la intención original [0.59ms]

 100 pass
 3 fail
 456 expect() calls
Ran 103 tests across 10 files. [108.00ms]

=== LINT ===
$ eslint .

/home/runner/work/SOLOG/SOLOG/src/features/solog/admin/catalogo/admin.catalogo.feedback.tsx
  6:17  warning  Fast refresh only works when a file only exports components. Use a new file to share constants or functions between components  react-refresh/only-export-components

✖ 1 problem (0 errors, 1 warning)


=== BUILD ===
$ tsc -b && vite build
[36mvite v8.2.1 [32mbuilding client environment for production...[36m[39m
[2Ktransforming...✓ 2029 modules transformed.
rendering chunks...
computing gzip size...
dist/index.html                                                        0.78 kB │ gzip:  0.39 kB
dist/assets/plus-jakarta-sans-vietnamese-wght-normal-qRpaaN48.woff2    8.35 kB
dist/assets/plus-jakarta-sans-latin-ext-wght-normal-DmpS2jIq.woff2    21.72 kB
dist/assets/plus-jakarta-sans-latin-wght-normal-eXO_dkmS.woff2        27.34 kB
dist/assets/detalles-qdWCahzf.css                                      7.27 kB │ gzip:  1.77 kB
dist/assets/cajero-axNPaxYa.css                                       13.19 kB │ gzip:  2.82 kB
dist/assets/cajero-CXRqGFMh.css                                       30.78 kB │ gzip:  5.15 kB
dist/assets/index-C9IfEHEo.css                                        34.19 kB │ gzip:  9.23 kB
dist/assets/admin.v2-BHD9Fx-1.css                                     64.61 kB │ gzip:  9.86 kB
dist/assets/check-CaTg6rf1.js                                          0.11 kB │ gzip:  0.13 kB
dist/assets/chevron-down-BHpvoXk1.js                                   0.11 kB │ gzip:  0.13 kB
dist/assets/chevron-up-inY9OgbN.js                                     0.11 kB │ gzip:  0.13 kB
dist/assets/chevron-left-CgIjo8-y.js                                   0.11 kB │ gzip:  0.13 kB
dist/assets/chevron-right-PrFiqyWk.js                                  0.11 kB │ gzip:  0.13 kB
dist/assets/x-C9nscKSt.js                                              0.14 kB │ gzip:  0.14 kB
dist/assets/search-CS0JKhvj.js                                         0.16 kB │ gzip:  0.16 kB
dist/assets/tablet-CsT0L7VV.js                                         0.20 kB │ gzip:  0.18 kB
dist/assets/circle-off-BNQbrOj-.js                                     0.23 kB │ gzip:  0.19 kB
dist/assets/refresh-cw-CYD53E09.js                                     0.31 kB │ gzip:  0.21 kB
dist/assets/package-B0JxRxga.js                                        0.36 kB │ gzip:  0.24 kB
dist/assets/admin.v2.format-c6HNLBD9.js                                0.37 kB │ gzip:  0.27 kB
dist/assets/admin.masterdata.context-D6XF4c-e.js                       0.48 kB │ gzip:  0.33 kB
dist/assets/admin.v2.presentation-B3KqmPlU.js                          0.56 kB │ gzip:  0.39 kB
dist/assets/package-open-ByxnnyAM.js                                   0.56 kB │ gzip:  0.32 kB
dist/assets/triangle-alert-DTLMW-Q0.js                                 0.73 kB │ gzip:  0.37 kB
dist/assets/send-C5SXLmTF.js                                           0.90 kB │ gzip:  0.48 kB
dist/assets/admin.management.presentation-BqA6lzMS.js                  2.12 kB │ gzip:  1.03 kB
dist/assets/admin.control.v2.export-B7-HS5Au.js                        2.22 kB │ gzip:  1.02 kB
dist/assets/admin.control.v2.export-dialog-DeYjKZQf.js                 2.58 kB │ gzip:  1.22 kB
dist/assets/admin.valuation-dialog-DI2QVk61.js                         3.05 kB │ gzip:  1.29 kB
dist/assets/admin.dialog-D728Rvtj.js                                   3.16 kB │ gzip:  1.42 kB
dist/assets/admin.product-setup.dialog-B1YWL3Iy.js                     3.90 kB │ gzip:  1.62 kB
dist/assets/detalles.export-CTCjfgJ0.js                                4.61 kB │ gzip:  1.96 kB
dist/assets/admin.dispositivos.v2-CH_O6Nb_.js                          6.15 kB │ gzip:  2.25 kB
dist/assets/cajero.utils-yoHsepzC.js                                   7.00 kB │ gzip:  3.06 kB
dist/assets/admin.dashboard.v2-CJfbJJaG.js                             8.34 kB │ gzip:  2.77 kB
dist/assets/admin.productos.v1-BkghJRKn.js                             9.33 kB │ gzip:  3.01 kB
dist/assets/admin.control.v2-m7xue4nK.js                              13.64 kB │ gzip:  4.24 kB
dist/assets/admin.incidencias.v2-DEoUOCxb.js                          17.24 kB │ gzip:  5.61 kB
dist/assets/admin.catalogo.page.v3-DjLQutn0.js                        18.32 kB │ gzip:  5.46 kB
dist/assets/admin.grupos.v2-BIrE5Kux.js                               23.97 kB │ gzip:  6.63 kB
dist/assets/protected-app-C--8bNGc.js                                 26.13 kB │ gzip:  8.50 kB
dist/assets/detalles-Czd7Old2.js                                      27.28 kB │ gzip:  7.87 kB
dist/assets/cajero.app-BF3m5DMw.js                                    28.22 kB │ gzip:  8.19 kB
dist/assets/admin.v2.app-BhqRyw_1.js                                  56.00 kB │ gzip: 14.37 kB
dist/assets/cajero-CwXDy3Vn.js                                        56.65 kB │ gzip: 14.68 kB
dist/assets/browser-CB9WaMml.js                                       70.71 kB │ gzip: 19.69 kB
dist/assets/index-C8f9h1HA.js                                        202.02 kB │ gzip: 63.81 kB

[32m✓ built in 294ms[39m

=== DIFF CHECK ===
